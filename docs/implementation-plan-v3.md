# Detent v3 — Implementation Plan (the plugin re-target)

Source of truth: `detent-prd-v3.md` (3.0-draft.1). This plan decomposes the MP0…MP4 milestone series of §13 into tickets `T-1##` (distinct from the v2 line's `T-0##`). It applies **PRDR-065** and depends on nothing outside the v3 PRD.

## §0. Working agreements (carry from v2, unchanged)
- **No-deviation (N-6).** A design change files a `tickets/prd-review/` PRDR **first**, the PRD amends, then this plan and code follow. PRDR evidence blocks quote the PRD verbatim and are never retro-edited.
- **Trailers.** Commits carry `Detent-Ticket: T-1##`.
- **Tests-before-code, where an oracle exists.** The v2 kernel *is* the referee, and its **553 passing tests are the referee's conformance floor** — MP0 may not regress one. v3's *new* surfaces (the R-* MCP boundary, the plugin, the model-driven loop) have **no oracle**; they are spec-first, tests derived from the PRD's ACs.
- **Live-key discipline (R-10).** Any ticket that spends against the user's `ANTHROPIC_API_KEY` (marked ⚡ below) is gated on explicit per-run consent and a spend cap. Never run unprompted.
- **Code standards live in `AGENTS.md`** — ≤300 code lines per `src/` file (≤600 in tests), no inline comments, modern-syntax floor — with the lint-enforced half in `eslint.config.js` (`max-lines`, `no-inline-comments`, `prefer-const`/`no-var`/`prefer-template`/`object-shorthand`/`eqeqeq`, `consistent-type-imports`, no enums, no default exports). The same file is what `readRules()` feeds Detent's own sessions at self-build (N-7).

## §1. What v3 reuses vs replaces
**Reused unchanged, behind the referee's MCP boundary:** the state machine (§7), budgets (X-1), gate classifier, flake filter, checkpoints (F-4), ticket store, verification adapter (§6), and `schemas/**`. These carry their v2 tests forward verbatim.
**Replaced:** the kernel run loop (`src/kernel/run.ts` orchestration) and the CLI verbs (`src/cli/*`) — split into a driver-agnostic **referee** (MCP tools) plus **two drivers** (headless deterministic loop; interactive model-driven loop) and the **plugin** shell.
**The load-bearing invariant (D-27/ARCH-1):** every `machine.apply` still derives its event from a validator or gate result — now asserted at the *tool* boundary, true regardless of which driver called the tool.

---

## §2. Milestones & tickets

### MP0 — the referee (extract the v2 kernel behind MCP; headless driver at full parity)
The de-risking milestone: prove the kernel runs **unchanged** behind a tool boundary, with the deterministic driver reproducing today's behavior byte-for-byte. No live key anywhere in MP0 — it runs against the existing mock/fixtures.
- **T-100** Referee MCP scaffold (R-1): tool server skeleton, tool registry, zod I/O schemas reusing `src/schemas/**`. *AC:* server lists exactly the R-1 tool set; unknown tool → structured error.
- **T-101** `next` + `claim` tools (R-2): ready-set from the ticket store; atomic claim honoring X-3 admissibility. *AC:* two-ready fixture — either claimable; blocked-ticket claim refused naming the blocker.
- **T-102** `record` + `transition` tools (the sole `machine.apply` path): each tool gate-/validator-derives its event; the ARCH-1 apply-site audit is **extended to the tool boundary**. *AC:* `transition` with an inadmissible event refused, no checkpoint written; audit test asserts no tool reaches `machine.apply` without a validator/gate result.
- **T-103** `gate` tool: run + classify a bound gate via the existing adapter + flake filter (F/V reuse). *AC:* the v2 gate/flake tests pass through the tool unchanged.
- **T-104** `attempt` tool (R-4): metered billable-session spawn wrapping the existing `SessionBackend`; ledger check-before / record-after; over-ceiling refusal routes to a human (P6). *AC:* over-budget fixture refuses and routes; ledger sums every session (mock backend).
- **T-105** Checkpoint/resume through the referee (R-3, D-30): every admitted transition persisted to `.detent/` before the tool returns. *AC:* kill mid-`attempt`; re-drive resumes from the last admitted transition; stale claim reclaimable (C-9).
- **T-106** The **headless driver**: re-express `run.ts`'s loop as a driver over R-* tools — implemented as an Agent SDK program loading **the same plugin directory** (`options.plugins: [{type:"local", path}]`), so one artifact serves both drivers. *AC:* byte-identical `transitions.jsonl` vs the v2 loop on the oracle crash-resume class; the fixture doubles as proof that plugin-bundled MCP + hooks behave identically under SDK load.
- **T-107** **MP0 exit** — the entire v2 suite (553 tests) green with the headless driver over the referee; ARCH-2 driver-agnostic audit; parity map annotated (the oracle tests now certify the referee). *Exit gate for MP1.*

### MP1 — the plugin skeleton
- **T-110** Plugin manifest (`.claude-plugin/plugin.json`) + `marketplace.json`. Component dirs live at plugin **root** (only `plugin.json` inside `.claude-plugin/`); `version` pinned explicitly (SEC-2); `userConfig` weighed as the home for spend cap + docs-domains (candidate resolution for PRDR-062). *AC:* `claude plugin validate --strict` passes; registers exactly two commands.
- **T-111** The two commands (`/detent:init`, `/detent:run`) as **skills** — `skills/init/SKILL.md`, `skills/run/SKILL.md` (directory form; `commands/` is legacy), `$ARGUMENTS` carrying flags like `--replan`. *AC:* both load in a live Claude Code session.
- **T-112** Vendored subagents: the role prompts (diagnose/implement/review/research) → `agents/*.md`, hash-pinned (S-7), using per-role `tools`/`disallowedTools`/`permissionMode`/`maxTurns` frontmatter for S-3′ surfaces. *AC:* `role@hash` resolves; runtime never fetches (SEC-2).
- **T-113** The D-21 containment hook as a plugin hook (`hooks/hooks.json` PreToolUse) + Stop-gate hook. Deterministic hook types only (`command` or `mcp_tool` calling a referee `guard.check` tool — one legality implementation, ARCH-2); the platform's `prompt`/`agent` hook types are P2-forbidden for containment. *AC:* the M0 guard tests pass against the plugin hook.
- **T-114 ⚡ MP1 exit** — via `claude --plugin-dir` (no marketplace needed): commands load and the hook denies an out-of-surface write in a **live** session. *Live-key gated.*

### MP2 — the model-driven loop
- **T-120** The model-driven driver: skill(s) that sequence `next → claim → attempt → record/gate → transition` over R-* tools, choosing among *legal* moves only (R-2). Loop persistence may use the **Stop-hook re-feed** pattern (officially shipped precedent: ralph-wiggum) rather than trusting the model to continue — it coexists with the S-2 stop-gate, since Stop hooks combine. *AC:* the model completes a single-ticket run over the referee against a fixture backend.
- **T-121** D-28 budget hook: deny ledger-bypassing ambient billable tools (direct `Task`/gate-running `Bash`); `attempt` is the sole billable path; the platform's native subagent budget caps layer underneath as defense in depth (Detent's ledger stays the source of record). *AC:* ambient spawn for Detent work hook-denied; ledger sees every attempt.
- **T-122** D-29 / SEC-6 hook authority over loaded settings — confirming the documented combination rule ("most restrictive applies: `deny` > `defer` > `ask` > `allow`"; hook-allow never bypasses denies) by execution, per P4. *AC:* a repo settings file allow-listing an out-of-surface write is hook-denied; a repo-authored *hook* answering "allow" does not preempt the guard's deny; no transition recorded in either case.
- **T-123** Cross-driver parity harness: a multi-ticket run under the model driver yields `transitions.jsonl` byte-identical to the headless driver for the same admitted sequence (scripted/fixture backend for determinism). *AC:* parity fixture green.
- **T-124 ⚡ MP2 exit** — a live multi-ticket model-driven run completes; budgets provably hard. *Live-key gated.*

### MP3 — init as a plugin
- **T-130** The seven init phases surfaced as command/skills; the five C-5 decisions as the plugin's presented interrupts (closed set). *AC:* interrupt set asserted at five; phase order matches C-4.1.
- **T-131** Approval (C-7) as a plugin presented decision (the `makeTtyApproval` role, re-surfaced). *AC:* approved/declined/deferred map to the same three outcomes; approval records who/when/plan_hash.
- **T-132 MP3 exit** — the golden-path docs test passes against the plugin surface (two commands, five decisions).

### MP4 — self-build + distribution (N-7)
- **T-140 ⚡** The headless driver self-builds v3 in CI — the permanent gate (D-16), now naming `detent-prd-v3.md`. *Live-key gated.*
- **T-141 ⚡** Marketplace publish + install smoke test — three channels in order: own marketplace repo (immediate; any git repo serves), submission to the reviewed community marketplace (security-scan queue), claude.com/plugins listing. Positioning per research C.2: lead with the four unclaimed axes; frame methodology tools (spec-kit, Superpowers, BMAD) as complementary upstream. *Live-key gated.*
- **T-142 MP4 exit / v3 release gate** — N-7 green on the v3 document; plugin installs and runs the golden path.

---

## §3. Critical path
`T-100 → 101 → 102 → 103 → 104 → 105 → 106 → 107` (MP0, the long pole — the referee must be proven at parity before anything drives it) `→ 110 → 111 → 113 → 114` (MP1) `→ 120 → 121 → 123 → 124` (MP2) `→ 130 → 132` (MP3) `→ 140 → 142` (MP4). MP0 is ~8 tickets of pure reuse-behind-a-boundary; the genuinely new risk concentrates in T-102 (apply-site invariant at the tool boundary), T-121 (budget hardness under ambient tools — OQ-A in practice), and T-123 (proving the two drivers agree).

## §4. Open risks carried from PRDR-065
- **OQ-A budget authority** is validated (or falsified) in **T-121**. If the hook cannot make budgets hard, the fallback is D-25's stated overshoot bound, documented, not silent.
- **OQ-B unattended runs** is answered by the headless driver (MP0) surviving as the CI path — already in the plan.
- **OQ-C D-22 replacement** is validated in **T-122/SEC-6**.
- **OQ-D resumability** is validated in **T-105** (referee-owned checkpoints) and **T-123** (cross-driver agreement).

## §5. Changelog
**3.0-30** — **the audit of phase 2, and four defects in its own fixes (PRDR-151).** **The worst was a permanent brick, and it was strictly worse than what it replaced.** X-1‴'s ledger rule refused any torn line that was not last — which sounds right, and ignores that `appendLedger` writes `JSON.stringify(row) + "\n"`, so a line torn mid-append has NO trailing newline and the next append concatenates onto it. Reproduced: one `kill -9` made the following run's first session cost invisible to the ceiling (the exact defect X-1‴ exists to close), and the run after that refused at startup **forever**, with no repair instruction — while `detent report`, which skips invalid rows, showed a healthy total on the very file blocking the run. The distinction that was wrong is not WHERE the damage sits but WHAT it is: text that is not JSON is a torn write and is skipped at any position; a well-formed object that is not a ledger row is a shape the writer cannot produce, and only that halts. The cost is an under-count of the row glued to the torn one — a lower bound, the safe direction, and the shape S-4 already takes. **The S-4″ fix did not fire the way its commit described, and its test used a shape production cannot emit:** `parseResultMessage` hard-codes `turns: 0` whenever telemetry is absent, so the zero-turn refusal always pre-empts the streak counter — while the test used `okResult({ telemetryParsed: false })`, whose `turns` defaults to 1. That is the unrealistic-fixture pattern, in the test written to close it, in the session that shipped V-6 to detect it. The test now uses the production shape and asserts the substance that does land: the ledger row carries `partial: "crash"` and the run halts. **`finalize` sat outside the driver's breach handler**, so a worktree merge conflict escaped `processTicket`, `loop()` rethrew it, and the run exited 1 with the generation left `in_flight` and `detent status` showing nothing wrong. It now closes the generation and stops with a summary naming the conflict; the ticket stays DONE, which is correct rather than a compromise — the work was implemented, gated and reviewed, and DONE is terminal by design (X-3 offers no edge out). What failed is integrating it, and the branch and worktree survive for a human. **And `readBindings` threw outside `runWithConfig`'s try**, rejecting the promise instead of returning a `RunOutcome`, so an invalid `bindings.json` printed a bare message with no machine-readable summary. Also fixed in passing: the run lock landed at `.detent/run.lock`, committed territory, and `finalizeDone`'s `git add -A` would have committed it — on another machine the host would not match and it could never be broken. **A coverage gap is recorded rather than papered over:** the driver-level routing of a finalize breach has no test, because forcing a conflict through the sequential loop needs two worktrees alive at once; `mergeWorktree`'s own half is covered. It belongs with PRDR-144. 878 tests green; lint, parity and prompt-hash clean.

**3.0-29** — **audit remediation, phase 2: silent failure and data integrity.** **V-1″ (PRDR-135):** `runScopedGates` returned `null` when no binding matched any requested slot and the caller read it as a PASS, minting a real `GATE_GREEN` carrying "no bound gates" — so a `bindings.json` deleted, gitignored or never committed sent every ticket to DONE with zero verification commands executed, merged each into the run branch, and exited 0. A corrupt bindings file correctly threw; it was specifically ABSENCE that failed open. No bound gate is now unverifiable, and `run` refuses at startup beside its config and approval preconditions. **X-1‴ (PRDR-136/147):** the spend ceiling was enforced against a figure seeded once at construction and incremented in memory, so two runs on one root each enforced the full cap and jointly spent past it — silently, because per-ticket claims correctly kept them off the same ticket. The launch gate re-reads the file now, the file is validated with the schema that wrote it (a string cost concatenated — `5, "5", 3` gave `"553"` — a negative subtracted, `1e999` refused every launch), and a run takes an exclusive root lock on the `O_EXCL` primitive the claim mechanism already proves, breakable only on a dead pid on the same host. NG4 stands: concurrent runs are not made safe, they are made to decline. **F-3′ (PRDR-137):** four artifacts were validated on write and cast on read. The slice cache advertised itself as a validated trust boundary while checking 3 of 11 fields, so a cache from an older build was a HIT that crashed `init` mid-PLAN with `TypeError: t.provides is not iterable`; ticket writes truncated in place on every transition and `readTicket` parsed OUTSIDE the guard that names the file, so one torn ticket took `ready`, `pool`, `status`, `report` and `doctor` down together with no filename; and both journal readers parsed unguarded — including `unfinished`, which exists to be read after a crash, which is exactly what tears the last line of an append-only file. **S-4″ (PRDR-138):** a stream ending with no result message parsed as `ok: true` with no `crashed` flag, so the ledger took a $0 row unflagged, the journal recorded a successful end for a session that died on the wire, and the success branch RESET the outage streak — a repeated backend outage could therefore never reach `CRASH_STREAK_HALT`, and the operator was told "budget breach", at $0, about an outage. **B-2′ (PRDR-145a):** `mergeWorktree` ran `git merge --no-ff` unguarded and removed the worktree and deleted the branch unconditionally, so two tickets touching one file left a ticket DONE with its work unmerged and an orphaned worktree, surfacing as exit 1; a conflict is now an outcome that keeps the branch, keeps the worktree, leaves the run branch unmoved and routes to the human. This is the precondition for PRDR-145b making worktrees the default. One test fixture was corrected rather than accommodated: it wrote `{ cost_estimate_usd: 1000 }` as a ledger row, a shape the product cannot produce, and was viable only while the reader was an unvalidated cast. 877 tests green; lint, parity and prompt-hash clean.

**3.0-28** — **V-6 (PRDR-150): a test that would pass without the change it ships with is not evidence, and Detent now checks that mechanically.** The motivating measurement: across the 7 September audit and its remediation, the original 3.1.1 code ran about one defect per 100 lines and the FIXES for those defects about one per 44 — written by an author actively hunting that class, on the same day, and landing in the code that matters by definition. Three of the audit's critical blockers had a PASSING test asserting the property they violated: SEC-4 asserted against a function production never called, the git-hooks evasion passed because the test's own policy carried the glob the product lacked, and the symbol reminder was only ever tested with an input production cannot produce. The remediation then repeated it — the SEC-5′ test passed because its fixture was already CI-safe, so the normalization mismatch it should have caught was a no-op inside it. All four are one mechanical property. After a green gate, when a ticket's diff touches both source and test files, Detent reverts the SOURCE half, re-runs the bound test gate (preferring `test_single`, the seam the flake filter established), and restores the tree; tests that stay green are named to the review beside A-1⁗'s contract evidence. **Evidence, never a gate** — the A-1⁗ precedent, for the same stated reason: a check whose false-positive rate has not been measured must not fail a ticket, and a test can honestly stay green because it guards against over-correction rather than reproducing a defect (this line shipped three of those, labelled as such). The probe's failure mode got more attention than its success: `git apply -R` refuses cleanly rather than half-applying, the restore is in a `finally`, a dirty tree makes it report "could not revert" instead of guessing, and a missed restore is bounded by B-5's existing dirty-tracked reset on the next claim. The path heuristic for test files is wrong in the harmless direction — a missed file means no probe, never a false accusation. 864 tests green; lint, parity and prompt-hash clean.

**3.0-27** — **the audit of phases 0 and 1, and what it found in the fixes themselves (PRDR-149).** Eight defects, all in code written the same day, several reproducing the exact harm their own commit message claimed to remove. **Two were critical.** The SEC-5′ drift check compared `binding.resolved` — the NORMALIZED command `bind.ts` stores — against `Candidate.resolved`, documented as the literal one before normalization; so `"test": "vitest"`, the default from `npm create vite`, bound as `npm run test -- --run`, discovered as `npm run test`, and **halted an untouched repository on its first gate**, unrecoverably, because `verify sync` re-binds through the same normalizer. The PRDR-134 test missed it because its fixture is already CI-safe, which makes normalization a no-op — the fixture-chosen-so-the-test-passes pattern, committed by the session auditing for it. And `status: "provisional"` returned `exempt` **before any comparison**, `exempt` is not in the halting filter, and `runScopedGates` never reads status — so one extra field in a committed `bindings.json` bypassed SEC-5′ whole. Being exempt from DRIFT (no baseline to have drifted from) does not mean the command need not be the one discovery found; one answer was serving two questions. **The surface lever, three ways:** `isConcreteRepoPath` was a BLACKLIST of `* ? [ ]`, and `"."` contains none of them while permitting every path in the tree — the exact bypass `**` was blocked for; picomatch's extglob and brace forms walked the same gap; `"./"` made `matchAny` compute an empty pattern and THROW on every later call, persisted onto the ticket; and `.detent` was granted as the floor's PARENT and then permitted the run's own spend ledger, because PRDR-132 unified the list and left the matchers apart. It is a whitelist now, with `coversProtected` asking the question the other way round. **The floor reached two policies and not the other two** — `sessions/live.ts` and `cli/referee.ts`, the construction fallbacks, had neither `.git` nor the structural set. **The env allowlist could not commit:** implement sessions hold `Bash(git commit:*)` and `GIT_AUTHOR_*`/`GIT_COMMITTER_*`/`GIT_CONFIG_GLOBAL` were stripped, so on the very subscription-CI path PRDR-148 was written for, every commit failed with "Please tell me who you are" — PRDR-148 fixed the transport and left the identity. **And the ambient hook was still steerable by repository content:** the re-feed string was echoed verbatim and unbounded into a `block` reason in every session of every plugin user who opened the directory (it now says Detent's own constant, pinned to the referee's by a test); an absent expiry was expired on the Stop path only, one function away from the PreToolUse path; and a committed symlink to `/dev/zero` — git stores mode 120000 quite happily — hung every tool call against a 900-second timeout, now bounded by a regular-file-and-size check. One proposed change was **reverted after weighing it**: treating an unparseable policy file as absent would remove a repo-plantable denial of service, but that DoS is loud and self-describing while a corrupted policy read as absent stops containing silently — fail-closed stays, and the reasoning is in the code. 858 tests green; lint, parity and prompt-hash clean.

**3.0-26** — **audit remediation, phase 1: containment and credentials.** **PRDR-133 is one line and closes the widest hole.** `buildSessionEnv` — SEC-4's session-environment allowlist, whose own header promises *"cloud credentials, tokens, deploy keys never cross into a session"* — had **zero production callers**: `buildOptions` never set `env`, and the pinned SDK inherits `process.env` when it is omitted. Every session, holding `Edit`, `Write` and `Bash(git commit:*)`, carried the operator's `AWS_SECRET_ACCESS_KEY`, `GITHUB_TOKEN` and `KUBECONFIG`, and `git commit -m "$AWS_SECRET_ACCESS_KEY"` matches the prefix allowlist. The test asserted the helper rather than `buildOptions`, so it was green throughout — the third instance of that shape on this line, and the reason the rewritten test asserts on the options a session is actually built with. The same missing call also dropped `EXTENDED_CACHE_HEADER`, so S-6's extended prompt-cache TTL had never once been requested by any run. **PRDR-132:** `.git` was in no protected set anywhere, and the surface lever granted wildcards — `handleSurfaceRequest` checked the model-supplied target against `config.protected` alone, never against the structural floor the same file builds for the policy, and never checked it was a path, so `**`, `.git/**` and `/etc/**` were all granted and PERSISTED onto the ticket for every later generation. `.git` matters because writing into it is executing: `.gitattributes` plus a `filter.*.clean` entry runs a shell command on `git add`, which the implement role holds and `finalizeDone` performs. The floor is now one shared list in `schemas/common.ts` — ARCH-1 forbids the kernel importing `sessions/**`, and schemas is the layer below everything — so the grant path and the enforcement path can no longer disagree; they used to, and a request for `.detent/config.json` was recorded as GRANTED while the write was separately denied. The evasion pack now asserts `.git` against the product's floor rather than a policy the test wrote for itself. **PRDR-134:** SEC-5 compared `config_hash` and never `resolved` — the only field that executes — while `config_hash` is computable from the repository's own config region, so a committed `bindings.json` could pair a validating hash with `curl … | sh` and the first gate ran it. The bound command is now compared to what discovery finds, and the message names it. **PRDR-146:** `installTrailerHook` overwrote any existing `prepare-commit-msg` at the start of every run; a foreign hook is now preserved under `.before-detent`, the caller is told where, and re-installing stays idempotent. Note it writes to `--git-common-dir`, so it reaches the main repository even in worktree mode — the one data-loss surface making worktrees the default will not cover. 845 tests green; lint, parity and prompt-hash clean.

**3.0-25** — **audit remediation, phase 0: the four critical blockers that were one-liners, and one self-inflicted defect.** Every fix landed with a test observed to FAIL first; the two that pass either way are labelled as over-correction guards rather than left to imply a reproduction. **PRDR-128 is the serious one and it was reproducible from a bare directory:** the bundled plugin hook is registered on `Stop` with no matcher, so it runs in every session of every user who installed the plugin, and it read `gate_cmd` out of `<cwd>/.detent/stage.json` — ordinary repository content — and ran it through a shell, while `expired()` treated an ABSENT `expires_at_ms` as eternal. Planting only that file, with no config, plan, approval or run in flight, executed arbitrary code as the operator. The filed remedy was three conditions (live expiry, run nonce, bound command); investigating the producer side changed the answer to **removal**, because nothing in the product has ever written a non-null `gate_cmd` — `refreshRunRefeed` hard-codes `null` and a test asserts it — so the execution path had no producer and served only an attacker. The four tests that made it look legitimate exercised a shape production cannot produce; they went with it. The re-feed is untouched, and an absent expiry is now expired. **PRDR-129:** `detent run` defaulted to the MOCK backend while `referee` defaulted to live and `init` refused the mock outright — and the README's golden path, test-locked to exactly `detent init` and `detent run` with no flag, is the whole public workflow. So the documented path ran a fake whose `okResult()` feeds fabricated telemetry into the real ledger and real journal events against real ticket state. Default is now live, a fixture run announces itself, and the per-run config event records the backend — `SessionBackend.name` existed with zero readers, so a journal could not say whether a run was real. **PRDR-130:** `git()` ran without `maxBuffer`, so Node's 1 MB default threw ENOBUFS above it (confirmed on the pinned v22.23.2) and every wrapper turned the throw into an empty value — one `null` for two facts. The reviewer got `""` for a diff, under the truncation cap so the banner never fired; `changedFiles` returned empty so B-4 minted no risk label; and `snapshotRefs` returned an empty map, after which the base guard's own "a new branch is also a write" loop matched EVERY local branch and deleted it, `main` included. Buffer is explicit, and `gitCouldNotRun` separates "ran and said no" from "could not run" on a discriminator verified against real error shapes. **PRDR-131:** the crash skip counted `start` against `end` over a ticket's whole journal with no generation scoping, and the skip event rebalanced neither — so one killed session suppressed that role forever and requeue could not clear it. The falsification is the clearest evidence in the set: without the fix the new generation launched `blind_fix`, `research`, `informed_fix` and never `implement`, burning real money fixing an implementation that had never been written. Session events now carry their generation, which was not recorded at all. **PRDR-131a:** a macOS-only assertion this session introduced hours earlier (`realpathNearest("/tmp")`) would have failed on the Linux runner CI uses; it now builds its own link. 829 tests green; lint, parity and prompt-hash clean. Phases 1–6 remain.

**3.0-24** — **PRDR-127: the containment guard judged the path a session typed, not the path the write lands on.** `path.resolve` normalises `..` lexically and follows no links, so a symbolic link inside the worktree produced a relative path that still looked inside — and every check downstream ran against a file the write would never touch. The audit that filed this expected a worktree-boundary problem. Measured against real links on disk it is **three**, and the boundary is the least of them: a link walked out of the tree entirely, out of the declared `surface[]`, and **through the SEC-3 protected globs** — a straight bypass of the immutability floor, which is the one guarantee that holds regardless of what a ticket declares. All three checks now run on the resolved destination, which is what makes the surface and protected repairs work at all; had only the boundary been resolved, two of the three would still have been `allow`. **The destination is judged, never the mechanism:** a link whose target is somewhere the session may already write stays allowed, because refusing links as a class would refuse `node_modules/.bin` and every monorepo workspace link. Three details decided rather than assumed: resolution walks up to the nearest ancestor that EXISTS, since `realpath` throws and a `Write` creating a new file is the ordinary case; **dangling** links are followed manually with `readlink`, because the Write tool creates parent directories and a link to a not-yet-existing directory outside the tree would otherwise be created and written to — a hole the first draft of this fix still had, and the probe caught; and BOTH sides are resolved, since `/tmp` is itself a link on macOS and comparing a resolved target against an unresolved root would have called every temp worktree an escape. The resolver is a parameter with a filesystem-backed default, so the seven oracle hook ports stay hermetic, and a resolver that throws denies. The evasion pack's tenth case is labelled **"symlinky nested traversal"** and is `/wt/src/a/../../AGENTS.md` — lexical `..`, no link anywhere; it asserted the thing it was named for and never tested it. Twelve new tests, ten of which were checked to fail without the fix. Left open and named: the TOCTOU window between the decision and the write, bounded by the kernel re-running verification (P2). 822 tests green; lint, parity and prompt-hash clean.

**3.0-23** — **the audit of the unreleased 3.1.1 line, requested after the gate run was stopped.** Six defects, all fixed with regression tests that were checked to FAIL without the fix. **Two shipped features did nothing.** The symbol reminder (S-3″/PRDR-121) could never fire in any real project: `symbols.enabled` defaulted to `false` in the config schema, the reminder honours `false` as "a person declined, never ask again", and `init` writes a config before it reads one — so every project looked like it had already declined, and the only input that produced the message was `undefined`, which the production path never emits. Its test passed because `undefined` is the only value the test ever passed in. `enabled` is now tri-state (absent ≠ declined) and the regression test goes through `loadConfig` the way `init` does. **And a whole-plan re-review that never ran printed `approve`:** `wholePlanReview` guards the FIRST review's absence carefully — the comment there says a human approving the plan could not otherwise know it never ran — then thirty lines later reads a null SECOND verdict as an empty finding list, so a plan redrafted for coherence findings and never re-checked reached the human as approved. The same hole, in the same function, in the half written later. **In C-2‴:** `normaliseDraft`'s rename map rewired edges away from the ticket that KEPT a name — when the planner drafted one id twice, the first kept it, the second was renamed, and the map then pointed every reference at the renamed copy; the plan stayed well-formed and simply meant something else. `renamed` is now the last resort, consulted only for names nothing answers to, and a duplicated id is a `coherence` finding. The slice cache defaulted `external_deps` to `[]` and `reviewed` to `true` — the two answers that always pass — while `sliceKey` hashes what a slice READ and not the code that read it, so an older cache matched its key and was read as reviewed; both are now required. Whole-plan redraft questions skipped PRDR-119's numbering, so several redrafted slices could each contribute a `q1`. Finally, four git-heavy tests failed as 5s timeouts under parallel load and passed on the next run; `testTimeout` is now 20s, ~16x the slowest observed test. The pattern, again: **fail-open defaults, and claims verified against their own assumptions.** Left open deliberately and named in the ticket rather than fixed in a sweep: the containment guard resolves with `path.resolve`, which does not follow symlinks. 810 tests green; lint, parity and prompt-hash clean.

**3.0-22** — **the audit of PRDR-120/121, and what it found in code written hours earlier.** Two adversarial reviewers plus a self-pass produced seven confirmed defects, every one now fixed with a regression test. **PRDR-122 (S-2‴) is the serious one and it was pre-existing:** the D-21 containment hook answered `allow` for any tool it does not govern, and a hook decision runs before every other permission step — so `allow` ends the evaluation before the allow rules are reached. `implement` is allowlisted exactly `Bash(git add:*)` and `Bash(git commit:*)`, yet every bash command passed, because a bash call names no path and fell through the blanket allow; the same hole waved through any MCP tool, including the editing tools PRDR-121 exists to keep out. The security pack asserted this behaviour on a line whose own comment named the allowlist as the real control. Fixed with a third decision — `abstain` omits `permissionDecision` so the allowlist decides — which removes the dependence on SDK precedence rather than betting on it. **In A-1‴:** derived edges were judged against a reachability snapshot taken before any derivation, so two tickets consuming each other's names both derived edges and deadlocked; and, worse, contracts knew nothing of slice order, so an early ticket consuming a name a later slice provides derived a backwards edge while `capstoneBlockers` added the forward one — an ordinary shared-config-key shape that stranded both tickets READY and unclaimable with zero findings. Both fixed, with slice order now defaulting to the tickets' own order because the permissive default was fail-open. Also: the edge and the note disagreed about which ticket owns a contested name (`resolveOwner` now shared), DONE work outside the current draft lost ownership of its names, and a trailing space split one name into two. **PRDR-123:** `symbols.command` accepted any path, letting a repository choose which executable the orchestrator runs — now a bare PATH name refused at load — and a session whose MCP server never attached said nothing, now read from the SDK init message and journalled like a model fallback. And the Serena launch flags were invented rather than checked (`--context ide-assistant`, `--enable-memory false`); upstream's real ones are `--context claude-code` and `--mode no-memories`, and the test asserting the fake flag passed while proving nothing. The pattern across all seven: fail-open defaults and claims tested against their own assumptions. 802 tests green; five gates clean.

**3.0-21** — **PRDR-121 complete: symbol intelligence wired end to end, and the interface a ticket promises is checked against the diff it produced.** The adapter shipped as a library in the previous commit and had no caller — three of its acceptance criteria were unmet and the ticket was wrongly marked DONE, then reopened. Now: `determineVerification` probes the configured command and raises `AWAIT_SETUP_CONSENT` naming the pinned `uv tool install` line when it is enabled and unrunnable (Detent still installs nothing — D-4/F-2); the referee probes once per run and grants implement and review sessions the READ tools plus the MCP server, with `assertNoEditingTools` refusing any allowlist that would let the server write outside the D-21 hook's sight, and `--enable-memory false` keeping replay deterministic (C-8/S-6). A-1⁗ adds the contract check the operator chose over a precise lookup: a declared `symbol:` provide whose identifier appears nowhere in the ticket's diff becomes `unverified_provides` in the review's inputs, with an instruction saying it is evidence and not proof. It needs no tooling at all, never fails a ticket and never touches the gate. Thirteen tests over the adapter, the reminder and the check, including that a longer identifier does not satisfy a shorter one and that an empty diff never accuses. All nine plan tickets now have real call sites in `src/`. 791 tests green; five gates clean.

**3.0-20** — **PRDR-120 (A-1‴): a ticket now declares the interface it owns, and Detent checks the coupling with code instead of waiting for a reviewer to notice.** ksar-cloud's walking-skeleton slice produced eight findings that survived a revision round, and five were one defect in different costumes — two tickets disagreeing about a name neither owned: `v1.TerminalStates()` fixed by one ticket and contradicted by another, `ErrTerminate` half-defined, an image name one ticket derives and another's validator rejects, a `Deps` composition wired from a ticket that builds it, and `controlplane/go.mod` edited by four tickets at once. The plan could express coupling only as `depends_on` (a planner's guess) and `surface` (a file glob), neither of which can say "t-002 defines TerminalStates() and t-016 depends on what it means". Now `provides`/`consumes` carry it, over a closed six-kind vocabulary (`symbol`, `config`, `file`, `route`, `table`, `event`), each provide carrying the meaning a consumer needs. `src/init/contracts.ts` runs four checks with no model and no judgement: duplicate ownership and contended files become `coherence` findings naming both tickets, an unowned name becomes a `dependency` finding, and **a provider the plan does not already order before its consumer becomes a derived dependency edge** — the plan carrying an edge the planner never wrote, which X-4′ would otherwise recover a generation later at run time. An edge that would close a cycle is refused and reported, because mutual need is a contradiction rather than an omission. The declarations travel onto the written ticket, so `publicTicket` hands an implement or review session its own `provides` and, per `consumes`, the OWNING ticket's note — the interface reaches the work instead of being inferred and contradicted. Ten new tests, each modelled on a real ksar finding, plus an end-to-end case proving a derived edge reaches the ticket file and the presentation names it as Detent's own. Scoped honestly: contracts mechanise the structural half of `coherence`; whether two rules over one value agree stays the reviewer's, now looking at the right pair. 778 tests green; five gates clean. PRDR-121 (symbol intelligence as an optional adapter, never installed by Detent) is filed and not yet built.

**3.0-19** — **PRDR-119: the first full-product run asked two hundred questions, and twenty-five of them were real.** The first live `detent init` over ksar-cloud's 29 documents cut the product into 19 slices and ~528 tickets, then planned slice one: 31 tickets, four sessions, $21.77, and ten questions — of which seven were the planner asking permission for decisions it had already made and justified from the documents (host networking as a slice-01 simplification, NATS auth on loopback until the mesh slice, whether one trusted host is acceptable — which the slice document itself requires). Projected across nineteen slices that is ~200 questions at approval against perhaps 25 a human must actually decide, which turns C-3′'s single sitting into a document nobody can act on. C-3″ fixes what earns a place in the batch: a question is only for a fact outside the documents AND outside engineering judgement — a price, a domain, a payment rail, a legal rule, a credential — and everything a competent engineer could settle is settled, with the decision and its reason recorded in the ticket's `description`, the slice's `rationale`, or the analysis's `assumptions`. Also fixed: the plan draft's `expected_output` never showed the question shape though the instruction demanded the field, and question ids collided — a slice's first and revised drafts each numbered from one, so `s01-q1` appeared twice with different content, and ids are now assigned over the merged set and kept unique across every stage. The run also confirmed the layer works where it counts: slice one's review returned twelve findings naming real interface contradictions (two tickets writing the same file and disagreeing about what `run()` does at startup; a ticket needing a config key added four dependencies downstream; a test placed in a package other than the code it exercises), each of which would have cost a generation at run time. Measured costs, for the record: ANALYZE $5.01, SLICE $4.85, a slice draft $7.07, a slice review $2.95, ~$21.77 and 50 minutes per slice, projecting ~$420 and ~16 hours for the full product. 768 tests green; five gates clean.

**3.0-18** — **PRDR-118: the slicing layer audited before its first paid run, and it was not bug free.** Asked to confirm PRDR-117 safe before pointing it at a five-hundred-ticket product, the audit found the layer had multiplied the places a model's output is taken at face value while the checks around them still assumed a single draft. Verified and fixed: a drafted ticket id is a FILE NAME and was unconstrained — `../../package` wrote outside the repository, two ids differing only in case were one file on macOS (the plan claimed three tickets, the disk held two, the pool deadlocked), and `plan`/`approval` overwrote the plan and approval artifacts (F-1′); a dependency cycle was detected by nothing anywhere and reached disk as a permanent silent deadlock (A-1″); `capstoneBlockers` treated a ticket's own edge into an earlier slice as satisfying that slice's order — the exact shape the planner is instructed to write — silently removing the ordering guarantee slicing exists for (C-2⁵); the in-flight refusal ran only for `--replan`, so answering a question in a document during a run and re-running `init` — the workflow PRESENT itself recommends — reset the ticket a session was working in (C-8″); `writePlan` created every ticket and deleted every orphan BEFORE validating, so a refused plan left the directory rewritten with no artifact, and it re-created a DONE bootstrap as READY, gating the whole project behind rebuilding finished scaffolding (C-8‴); the strict SLICE and PLAN artifacts had no relaunch while the far simpler review had one, giving a twenty-slice run better than a one-in-three chance of aborting hours in on a single stray key (C-4⁗′); a session that died in transport returned success with $0 recorded (S-4′); and a corrupt config silently widened planning scope to the whole repository (R-9′). Also: slice cache keys narrowed to what actually determines a slice — C-2‴'s incrementality claim was false as written, since the whole ANALYZE artifact sat in every key and ANALYZE re-runs and differs on any edit — plus validated cache reads, a `coherence` finding when the whole-plan review returns no verdict, questions kept across a revision, plan-wide findings no longer broadcast to every slice, and SLICE now reporting the slice count and session range before PLAN spends. New coverage: `tests/init/plan-write.test.ts` (nine cases over a write half that had none) and `tests/init/slicing-scale.test.ts`, which plans 500 tickets across 25 slices and asserts the graph is acyclic, fully resolved and executable, with tripwires on session count and input growth. 766 tests green; five gates clean. **Still 3.1.0 on disk: the D-16 gate has not run on this build.**

**3.0-17** — **the 3.1.1 line (unreleased, local main): the planner plans the whole product, and `init` no longer stops until the plan exists.** PRDR-117 (C-2‴ / C-2⁗ / C-3′): a `SLICE` phase between DETERMINE_VERIFICATION and PLAN cuts the whole document set into ordered increments — walking skeleton first, every requirement id placed once; PLAN plans each slice in turn with the earlier slices' ticket index in view (ids `t-<slice>-NNN`), reviews each as its own plan, then a fresh session reviews the WHOLE plan under a tag set that gains `coherence`, with one targeted revision of the slices it faults and the ids later slices depend on kept; slice order is enforced through capstone blockers in the written plan; each slice is cached under `.detent/state/plan/<slice>.json` keyed by everything it read, so an edited document re-plans its slice and reuses the rest (C-8) while `--replan` wipes the cache (C-8′); the draft's ids and edges are normalised rather than trusted (a colliding id renamed, an edge to nothing planned dropped as a `dependency` finding). Every question — ANALYZE's, SLICE's, each slice's — rides to PRESENT with the assumption the plan proceeds on and is asked once, with the plan; `AWAIT_INFO` moves to PRESENT and fires only for a blocking question, after the plan is written. A fifteen-item **production baseline** (secrets, auth, validation, rate limits, TLS, backups with a tested restore, migrations, health checks, structured logs, metrics, graceful shutdown, dependency audit, CI gates, incident runbook, accessibility) is placed by SLICE and delivered by PLAN as `baseline:PB-###` tickets unless `config.plan_baseline` is `none`. Found on ksar-cloud: twenty-seven tickets for a product the user sized at five hundred. Also on this line, all found by execution and filed first: PRDR-102 (`oversized.json` → `TICKET_OVERSIZED`, `sizing_evidence` to PLAN and REVIEW_PLAN), PRDR-103 (the `dependency` tag), PRDR-104 (headless runs write no hook files), PRDR-112 (the outage streak backs off and `OUTAGE_REQUEUE` returns its victims), PRDR-114 (an opinionated `model_routing` — planner on Fable 5.1, judgement roles on Opus 5, volume roles on Sonnet 5 — with an unavailable-model fallback; agent-sdk 0.3.258), PRDR-115 (greenfield bindings from the documents' own commands), PRDR-116 (verdict synonyms read as meant; one review relaunch). **Version stays 3.1.0: 3.1.1 ships only after the D-16 gate runs green on this build ("No green, no release").**

**3.0-16** — **v3.1.0: the release that removed every stop that never changed an outcome, certified by its own green.** **The D-16 gate: 67/67 DONE, exit 0 — $598.36 across 303 sessions, 78 generations, 538 journaled transitions, 59 of 67 tickets first-generation** (evidence repo `detent-n7-310`, kept; 66 planned tickets plus the bootstrap ticket the build writes itself). The gate ran across several builds, because its findings were fixed and shipped while it ran — every change confined to stop and consequence paths, so a ticket that never stopped ran identically throughout, and the last seven tickets ran on the final build; **the user certified the release on this evidence** rather than a fresh re-run, recorded here. What the release carries, all found by execution and filed first (N-6): the vendored role variants are gone after a null A/B (PRDR-089/093 — generations-to-DONE 1.00 vs 1.00 on every clean pair); the review basis is the ticket's OWN commits plus the uncommitted tree (PRDR-094), and is scoped by Detent's matcher rather than git's pathspec (PRDR-113 — git's `:(glob)` has no braces, so a granted `src/cli{.ts,/init.ts}` was invisible to the reviewer who rejected the exact commit every review had asked for); a run of $0 crashes is an outage and halts by name (PRDR-090); `doctor` reads the installed SDK version without crashing (PRDR-096); the ledger records the model that ran and the run journals the config it loaded (PRDR-095, PRDR-092 partial); a terminated session's untracked debris is parked under `.git/detent-parked/` at the next claim instead of failing every later ticket's gate (PRDR-100); `non_goals` reach the written ticket — the writer had dropped the field for the entire life of the plan pipeline, and the draft-to-ticket mapping is now total at compile time (PRDR-101). **Then the stops.** Counted on this gate: 29 human stops, 28 of which were a click or a verbatim relay. The turns ceiling fired four times (103, 145, 307, 222 turns), caught zero runaways, and every breached ticket finished on resume in 46–63 turns — **deleted** (PRDR-106, X-1″; `turns_per_stage` survives as the planner's sizing target, enforced nowhere). The plan's `risk_label` stopped eleven times across every gate and field test, approved eleven times, declined never — **advisory** (PRDR-107, B-4′; the operator's `risk` globs still gate). `review_fix_attempts` parsed any number and the guard was hard-wired to one round — **honoured, default 3** (PRDR-108, X-1‴). A reviewer that wrote no verdict was a budget breach — **relaunched once** (PRDR-109, A-5′). Dry research went straight to a human, eight of nine times — **enters the informed attempt** (PRDR-110, X-2′). And a falsification that names the code it is missing is a **dependency, not a stop** (PRDR-111, X-4′): `falsified.json` may carry `missing`, the referee resolves the paths against every other ticket's surface, the ticket waits in `waits_on` and returns to the pool by itself — proven live on t-112, which falsified naming `src/cli/plumbing/status.ts`, waited on t-154 and t-155, was re-claimed one second after the second landed, and reached DONE through two former stops with no operator note. The last ticket, t-164 (the N-7 gate ticket itself), found that 66 tickets had built every module and none had wired the CLI entry — and wired it itself through a SEC-3 grant. **Compatibility:** the computed worst case moved 14 → 24 and the default net `sessions` 18 → 28, so a committed config pinning `sessions` at 24 or below is refused at load with the R-9 message; additive persisted fields (`models`, `waits_on`) carry defaults and `schema_version` stays 1, per the PRDR-095 precedent; `DEPENDENCY_DISCOVERED` joins the closed event set. Filed for the next line: PRDR-102 (an oversized-ticket signal), PRDR-103 (a criterion reaching a later sibling), PRDR-104 (the hook keys on the shell's cwd, not the session's role), PRDR-112 (an outage halt still needs a person to restart it and requeue its victims). Suite: **722 passed / 2 skipped** (66 files).

**3.0-15** — **v3.0.1: the claim-discipline release, gated by its own green.** The release carries C-12′/C-9′: `detent unclaim <id> | --stale` (PRDR-078 — the explicit lock-release verb, live-owner refusals intact) and the pool's resume self-heal (PRDR-079 — a dead-owner claim on a resumable ticket releases at acquire, noted and journaled, ending the hand surgery every crash used to demand), plus the day's field findings: the reviewer's `operator_record` input (PRDR-080 — grants and guidance are visible to judgment, so sanctioned work stops reading as scope creep) and the probe classifier's two amendments (PRDR-076 — interpreter absence must NAME THE INVOCATION; compiler diagnostics and red-suite output bind red as they always did). **The D-16 gate re-ran fresh for the bump and came back green: 14/14 DONE, exit 0 — $50.69 across 67 sessions, 23 generations, 122 journaled transitions** — and the gate proved its own cargo in flight: PRDR-079's first real firing healed t-105's orphaned claim at resume (`stale claim released at resume: owner n7 pid 19432 dead`), and PRDR-080's record ended a live scope-flag loop on an operator-granted fix. Also on the gate's tape: the full X-2 ladder walked end-to-end on t-109 (red → blind → red → RESEARCH → informed → green), a genuine reviewer correctness catch on t-110 (the n7 build's verifySync requeued ALL of BLOCKED; the two entrances — gate-drift and UPSTREAM_BUG — now discriminate), and two usage-limit outage bursts that cost $0: every crashed session ledgered at zero, escalated honestly, and recovered by requeue. Checklist walked in order; the installed plugin's own hook, skills, and agents active on this machine stand as the install smoke's living evidence. Suite: **672 passed / 2 skipped** (60 files).

**3.0-14** — **T-141 channel 1 LIVE; T-142 GREEN — the v3 plan is complete.** The publish walked the T-142 checklist in order: five gates green locally and in CI on both Node lines; porcelain freeze re-affirmed by the docs tests; `plugin:validate --strict` ×4; pins coherent; SEC fixtures green with the T-124 live SEC-6 evidence still valid (CLI unchanged at the 2.1.235 pin); no persisted-shape changes. The v3 PRD promoted to **3.0, Released** — the draft line closed by the N-7 green. `release.yml` ran the gates and published **v3.0.0** on `main`; the repo went **PUBLIC** (MIT, PRDR-074); the marketplace manifest verified serving **anonymously** (a stranger's `claude plugin marketplace add AmineYagoub/detent` resolves); and the install smoke ran against the public channel — `detent@detent` installed clean at user scope. Two boundary notes for the record: the operator model was permission-blocked from the visibility flip until the user explicitly re-authorized, and hard-blocked from local plugin-config commands entirely — the user ran the smoke's install themselves, which is the R-10 shape anyway: the consequential clicks stay human. **T-142's exit line — N-7 green on the v3 document; the plugin installs and runs the golden path — is met** (3.0-13; this smoke; T-124's live model-driven run). Remaining under T-141's record, external by nature: the reviewed community-marketplace submission via `platform.claude.com/plugins/submit` (the user's account; PRs to the community repo auto-close) and the claude.com/plugins listing that follows its acceptance. **MP0…MP4 all shipped. Detent v3.0.0 is released, public, and installs from its own marketplace.**

**3.0-13** — **T-140 ⚡ N-7 GREEN: `detent init && detent run` on a PRD-only folder built the walking skeleton to 16/16 DONE — exit 0.** The evidence: a durable `detent-n7` repo seeded with only the two PRD documents (v3 + its inherited v2 closure); the harness stood in for the human at exactly the sanctioned points (git init, `--spend-cap-usd`, the T-131 approval relay), and every other stop was an honest red answered through C-12 plumbing (`requeue`/`approve`) with guidance recorded per X-8. Final ledger: **$59.19 across 97 sessions** (cap raised $40 → $100 by the operator, committed in-repo — B-5 resets uncommitted operator edits, learned live), **27 generations over 16 tickets** (eleven requeues, prior generations immutable throughout), **165 journaled transitions**, the built repo's own suite **534 passed**, its README two-command golden path authored by the build itself (t-112), and zero manual code edits inside the n7 tree — every operator act was a committed plumbing decision. **The honest reds were the yield**: five PRDRs' worth of kernel/prompt defects no mock could reach, each fixed test-locked in this repo before the next firing. **PRDR-069** — the claim base survives resumes BY DESIGN, so a stopped-and-resumed run interleaved foreign finalizes into the review span (a reviewer judged 4,695 lines for a ticket that owned 13 files); the basis is now surface-scoped, the ownership filter the plan already guarantees disjoint. **PRDR-070** — B-5's gate judges untracked work, `git diff` never showed it (t-107's worker wrote the production code without `git add`; the reviewer honestly reported "no production code"); reviewer and gate now judge the same tree, untracked files rendered as new-file pseudo-diffs. **PRDR-071** — a bare `slice(-8000)` fed four consecutive verdicts the span's TAIL (t-103's criterion-3 tests sat clipped off the front while every review cited "the two test files"); truncation is now loud — banner, complete file list including untracked names, 32KB bodies. **PRDR-072 + amendment** — a mid-run usage-limit outage turned four sessions into $0/zero-turn no-ops the machine marched through (gates re-greening unchanged trees, a refused reviewer replaying the previous verdict one second after launch); crashed-with-zero-turns now halts as a named `SessionRefusal` after the honest $0 ledger row, every real launch deletes its stale artifact first, and the crash-wrap reports OBSERVED stream turns — so a maxTurns crash marches with its real work (the amendment's live catch: thirty turns misread as zero) while only true refusals halt. **PRDR-073** — two live scope canaries (t-112's README criterion, t-113's unbuilt scrub/env behavior) escalated correctly instead of widening silently, but their lever requests died as empty-target denials: the implement prompt now names the request schema (`{"path", "justification"}`) and grant timing. Also exercised live, working as designed: **B-4** risk approvals ×5 (t-102/t-107/t-108/t-109/t-113, each review-approved first), **X-4** falsification-as-signal ×3, **X-3** refusing the operator's own out-of-protocol requeue (READY has no requeue row), and **SEC-3** deny-or-escalate on every scope probe. Suite: **660 passed / 2 skipped** (58 files). **T-140 is green; the D-16 gate stands permanent.** What remains of MP4: **T-141 ⚡** — the repo is PRIVATE by explicit choice, and every publish channel (public repo → community submission → claude.com listing) is the user's decision; **T-142**'s exit closes when it lands.

**3.0-12** — **the live-backend auth gate covers the subscription transports (T-140 unblocked for Max-plan users).** The v2 line hard-required `ANTHROPIC_API_KEY` — written when the API key was the only auth the platform had. Proven by execution on this Max-plan machine with the key deliberately absent: the Agent SDK's bundled runtime completed a real session on the claude.ai login alone ($0.03 probe). `hasLiveBackendAuth` (src/sessions/live.ts) now recognizes all three transports — the env key, `CLAUDE_CODE_OAUTH_TOKEN` (minted once with `claude setup-token`; the subscription CI path), and the claude CLI's own login (probed via `claude auth status`) — with `DETENT_NO_LIVE=1` as the forcing seam that keeps the harness dry-run spend-free even on a logged-in machine (env deletion cannot hide keychain auth; the previous dry-run design would have gone live on this very machine). `init`'s refusal names all three fixes; the harness's R-10 gate uses the same predicate; `self-build.yml` accepts either repo secret and its refusal names `claude setup-token`. R-10's GATE — explicit consent plus a cap — is unchanged; only the auth transport broadened, the same class of platform-fact refinement as `BILLABLE_SPAWN_TOOLS`. One docs-lock catch during the change: a checklist reflow split "No green, no release" across a line break and the T-142 test refused it — the phrase is load-bearing and now sits on its own line. Suite: **647 passed / 2 skipped** (55 files, +3). Net effect: **T-140 can fire locally with no further setup** (the logged-in CLI is the credential), and in CI with one `claude setup-token` secret.

**3.0-11** — **MP4's keyless body lands: T-140/T-141/T-142 prepared to the R-10 line; the two ⚡ firings wait on the user by construction.** The preparation surfaced — and closed — three real gaps the never-run v2 live exits (T-051/T-070) had hidden on the live path: **(1) `detent run` refused every non-mock backend** with a stale "lands at T-046" message — now wired: `src/sessions/live.ts` builds the ClaudeCodeBackend (bound test command as the Stop-gate accelerant, X-1 gate timeout), shared verbatim by `run --backend claude` and the N-7 harness. **(2) The per-ticket D-21 policy never reached the SDK hook** — the backend's guard policy was construction-fixed, so every live worker session would have run under one broad policy: `SessionSpec` now carries `policy?` (S-2′), the referee's session arm publishes per-spec `{surface: ticket.surface + ONLY `.detent/runs/**`, protected: config globs + a STRUCTURAL SEC-3 floor (tickets, config.json, bindings.json, plan/**), workRoot: workDir}`, and `buildOptions` prefers it over the constructor's — with an in-flight audit catch: the first draft granted `.detent/**` wholesale, which would have let a session raise its own budgets or rewrite its gates; the shipped shape narrows the surface instead. Worktree-mode live runs (absolute artifact paths vs a worktree workRoot) are recorded as an open interplay, worktrees defaulting off. **(3) `init` never wrote `config.json` at all** (the fixture wrote it by hand; config load refuses without it): `src/init/config.ts` — idempotent `ensureConfig` writing budgets `{run_spend_usd}` from the new **`--spend-cap-usd`** argument (an input like `--replan`, NOT a sixth decision; X-1 quoted: no defensible universal default, so a first init without it exits 2 naming the flag), the F-1 protected defaults, empty risk/routing, and S-5 pins (`agent_sdk` = the exact package.json dep; `claude_code` = the installed CLI at init time, "unknown" when unreadable — doctor flags it honestly). **T-140**: `scripts/self-build.ts` — N-7's harness: PRD-only folder (`detent-prd-v3.md`, the name the gate names) → `git init` (the harness stands in for the human exactly where the CLI routes to one) → `init --spend-cap-usd` → the **T-131 approval relay** (`--approve --by n7-self-build` — MP3's channel is precisely what makes a scripted non-TTY self-build possible) → `run` with the live backend → exit + journal row count; any non-approval interrupt is an honest red. `--dry-run` withholds the key even if the environment has one and proves the whole wiring up to the R-10 gate — CI-tested, spends nothing. `.github/workflows/self-build.yml`: **workflow_dispatch ONLY** (test-pinned: no push/pull_request/schedule trigger can ever exist), cap as a typed input, loud refusal without the repo secret, journal uploaded win or lose — the click plus the secret ARE the consent, so the plan's ask-first rule is enforced by the pipeline's shape, not by memory. Assumption recorded for the live firing: the Agent SDK's bundled runtime serves the sessions (no global CLI install step); if the first dispatch says otherwise it will say so loudly. **T-141 (keyless half)**: README leads with the four unclaimed axes per research C.2, frames the methodology layer as complementary upstream, states N-7 as the permanent maintenance answer, and documents the marketplace install — all outside the frozen golden-path section, docs-tested; the local install smoke stands executed from T-124 and is a per-release checklist item. **T-142**: `docs/release-checklist.md` — gates on both Node lines, the porcelain freeze, strict validation ×4, the install smoke, **N-7 with "No green, no release"**, pin coherence, SEC fixtures, F-3 schema discipline; docs-tested. Suite: **644 passed / 2 skipped** (54 files, +12), all gates green. **What remains of MP4 is exactly the two user-gated firings**: T-140 live — add `ANTHROPIC_API_KEY` as a repo secret and dispatch `self-build` with a cap (or run the harness locally with the key exported); T-141 live — the repo is PRIVATE by explicit choice, and publishing any channel (public repo → community submission → claude.com listing) is the user's call. T-142's exit closes when both are green.

**3.0-10** — **MP3 lands complete: T-130…T-132 (no live leg — the exit is the docs test, and it passes).** T-130: `skills/init/SKILL.md` is now the init program in full — the seven C-4.1 phases named in order, and the five C-5 decisions each documented at its bracketed phase with its TRUE answer channel, verified against the implementation before writing: AWAIT_DOCS → supply/name docs + re-invoke; AWAIT_INFO → answers go INTO the planning documents + re-invoke (C-8 replays from changed contents); AWAIT_BINDING_CHOICE → present candidates verbatim, the human disambiguates the repo's own tooling (V-1: never a guess), re-invoke; AWAIT_SETUP_CONSENT → present the proposal verbatim, consent is human-only, allowlisted, and logged (C-6a/SEC-1 — the engine exists; no consent flag is claimed because none is wired); AWAIT_APPROVAL → the T-131 relay. The skill drives the headless entry from `${CLAUDE_PLUGIN_ROOT}` (a plugin session has the checkout by construction), states the live-backend requirement honestly, and forbids the model from answering any decision or touching `.detent/`. T-131: `makeTtyApproval` re-surfaced as `makeFlagApproval` — the plugin path's approval transport: the model presents, the human answers in chat, the model relays exactly one of `--approve --by <name>` / `--decline` / `--defer` on the C-8 re-invocation; same ask seam, same three `ApprovalDecision` outcomes, approval recorded with who/when/plan_hash (asserted through the real `presentStage` + `approvalSchema`), declined stays READY-unapproved, deferred hands to the first `run` — both return the AWAIT_APPROVAL interrupt, where the wording lives. Flag rules pinned by test: more than one decision flag refused before anything runs; a relayed answer does not bypass C-1 (non-git root still exits 2 with no `.detent/`). No new porcelain: two commands, five decisions, and the C-5 readline scan's sanctioned set are all unchanged — the flags are an answer CHANNEL for one closed decision, exactly the C-1′ `$ARGUMENTS` pattern. T-132/MP3 exit: the golden-path docs test now asserts the plugin surface FROM THE SCHEMAS — exactly two skills; the init skill's `AWAIT_*` tokens set-equal to `INTERRUPTS` (a sixth would fail, C-5's closed set); the seven `INIT_PHASES` present in order; each decision documented at its `INTERRUPT_PHASE` position; the three approval outcomes + relay flags named. Trust note, recorded: `--by` is declarative, the same trust class as the TTY path's `$USER` (C-7's who has always been an honest declaration, not an authentication). The live end-to-end (a model driving real init sessions over a real key) is not MP3's exit and folds naturally into v2's open T-051 / MP4's T-140. Suite: **632 passed / 2 skipped** (52 files, +10), all gates green, `plugin:validate` ×4.

**3.0-9** — **T-124 ⚡ MP2 exit: GREEN — MP2 complete.** Live evidence (CLI 2.1.235, installed-plugin hooks+skills, harness referee via `--mcp-config --strict-mcp-config` with the GREEN-scripted mock backend — the model is the live half, the workers stay fixture-backed; full live workers remain T-140/N-7 — nine haiku sessions, ≈$0.47 total): **(1) the live multi-ticket model-driven run completes** — `/detent:run` executed by a real model drove t-1 and t-2 through claim → transition → attempt → gate → transition → review record → transition → close-check → transition → finalize → close → release, 30 referee tool calls in exactly the skill's order, outcome `ok`, both DONE — and the run's `transitions.jsonl` is **identical row-for-row (at-stripped; two wall clocks) to a headless twin**, which together with T-123's fixed-clock byte-identity discharges the §13 exit sentence. **(2) Budgets provably hard**: on an exhausted-ledger fixture the model's `attempt` was refused BEFORE any spawn (ledger still exactly one row), the model followed the breach route verbatim — record breach → transition → `BUDGET_BREACH → NEEDS_HUMAN` journaled, dossier written — then presented the four C-10 choices and STOPPED, holding the claim; in print mode that abandoned claim is precisely D-30's interactive-abandonment resume case, observed live. **(3) Ambient bypass denied live**: gate-command Bash, a file write (D-27 reason), and billable spawns all refused in-session under an active policy. The spawn leg found a real gap: this build's print sessions expose NO tool named `Task` — the live spawn surface is `TaskCreate` (background tasks) and `Agent` — so `deny_tools: ["Task"]` guarded yesterday's platform. Fixed at the referee (`BILLABLE_SPAWN_TOOLS = ["Task", "Agent", "TaskCreate"]` in hook-policy.ts; reads/controls stay allowed), then re-proven live: both new names denied with the D-28 reason. The hook itself needed no change — the deny list is policy data, which is the design working. **(4) SEC-6 platform merge confirmed by execution**: a repo shipping allow-everything permissions AND a repo-authored PreToolUse allow-hook (proven to have RUN via a marker file it wrote), under `--permission-mode acceptEdits`, still could not move the guard — write denied with the D-27 reason, no file created, no transition recorded. **Platform fact resolved**: 2.1.235 surfaces installed-plugin MCP tools in print-mode sessions (all eight, `mcp__plugin_detent_referee__*`) — 3.0-7's gap was version-bound; note the plugin-bundled referee still defaults `--backend claude`, which is why the deterministic legs ran the harness server. One incidental: a first SEC-6 attempt used "pwned" as the payload and the inner model refused on its own suspicion before any tool call — fixture prompts stay innocuous. **MP2 exit criteria per §13 are met.** Next: MP3 (T-130…T-132, keyless) or MP4; v2's T-051/T-070/T-071 (live workers) remain open alongside.

**3.0-8** — **MP2's keyless body lands: T-120…T-123; T-124 ⚡ stays open as the live exit.** T-120: `skills/run/SKILL.md` is now the model driver's full program — the state→move table mirroring the deterministic loop (attempt/gate/record/transition per state, the D-13 escalate-reason literal, BREACH and DRIFT_HALT as routes, terminal bookkeeping, escalation presented to the human and never answered for them), plus the referee-written **hook-policy files** the 3.0-6 obligation called for: `acquire` publishes `.detent/active_surface.json` (driver form: `driver: true`, EMPTY surface — the driver sequences, never edits — D-27 made mechanical at the hook), `releaseTicket` and the drift unwind clear it, and `pool()` maintains `.detent/stage.json` as the run-scoped **Stop re-feed** (ralph-wiggum precedent): standing while work remains, gone the moment it is not, one firing bounded by `stop_hook_active`. Both files are TTL-bounded by the X-1 wall-clock ceiling (`expires_at_ms`; the hook treats an expired policy as absent) so a crashed driver cannot leave a repo's ordinary sessions denied — the residual is a ≤1 h stale-deny window matching the claim's own staleness, documented. Both files joined F-1's LAYOUT as `local`/`run-level` entries, so the derived `.detent/.gitignore` keeps them out of ticket commits by construction (no PRD decision touched; flagged for the eventual §5 text refresh). T-121: the bundle now enforces D-28's ambient-bypass denies from the policy file — `deny_tools: ["Task"]` (attempt is the sole billable path) and `deny_bash_containing` = the bound verification commands (substring match on the Bash command; deny-only accelerant, so a false positive is a safe refusal and an evaded string still moves nothing — gates only classify through the referee). Known honest bound: wall-clock hardness under the model driver rides the policy TTL + the referee's ledger checks, not a driver-side timer. T-122 (keyless halves): a hostile-repo fixture — settings allow-listing everything plus a repo-authored allow-hook — cannot move our hook (denies unchanged, nothing written, and a source scan pins that the hook and guard never read a settings surface); the platform-merge half (documented deny-beats-allow combination) is execution-only and sits on **T-124's live checklist**. T-123: `tests/plugin/skill-driver.ts` — a scripted model driver that executes the SKILL's program literally over `callTool`, deliberately independent of `kernel/driver.ts`, and **now inside the T-102 drivers-hold-nothing audit scan** — drives twin repos against the headless driver under a fixed injected clock and the same run id: **`transitions.jsonl` is byte-identical**, timestamps included, on a two-ticket run (and a single-ticket run completes to DONE using only the eight R-1 tools). One fixture correction forced by the new referee clock reads: the T-041 wall-clock fake armed itself by CALL ORDER ("first reading arms"), which referee-internal TTL reads now shift — re-expressed as stage-armed time (the implement attempt consumes the ceiling), same oracle claim, no ordering coupling; BUDGET_BREACH is legal from every non-DONE state so the assertion set is unchanged. Docs locks added: the skill body must name every state the harness handles, and the D-13 literal must be ONE string across driver, harness, and skill. Suite: **622 passed / 2 skipped** (51 files, +19), all gates green, `plugin:validate` ×4. MP2 exit criteria remaining for **T-124 ⚡**: a live multi-ticket model-driven run (the skill executed by a real model over in-session referee tools — includes resolving 3.0-7's platform fact that plugin MCP tools did not surface in print-mode sessions), the live over-budget refusal routing to a human, the live ambient-bypass deny, and the SEC-6 platform-merge fixture.

**3.0-7** — **T-114 ⚡ MP1 exit: GREEN — MP1 complete.** Live evidence (CLI 2.1.233→2.1.235, claude.ai auth, eight haiku print-mode sessions, ≈$0.24 total): **(1) commands load** — debug log `Loaded 2 skills from plugin detent`; `/detent:init` invoked live applied C-1 by name (refused a non-git dir, created nothing, routed to the headless driver) and `/detent:run` presented the fixture plan faithfully and refused to sequence without the referee, exactly as its MP1 body instructs. **(2) The hook denies an out-of-surface write in a live session** — with `.detent/active_surface.json` planted (`src/**`), a root-level `notes.md` write was refused with the SEC-3 reason verbatim and the file never created, **under `--permission-mode acceptEdits`** — a live demonstration of D-29's deny-over-allow; the in-surface control (`src/notes.md`) wrote clean, proving the deny is surgical. **(3) The cwd assumption holds** — `claude mcp list` from a fixture project shows `plugin:detent:referee ✔ Connected`, and since `src/cli/referee.ts` exits before serving when `--root` (default cwd) has no config, a completed handshake proves plugin MCP servers spawn with cwd = the user's project. Marketplace path exercised for real: `claude plugin marketplace add <repo>` + `claude plugin install detent@detent` both clean (then uninstalled to restore the user's config). **Three platform facts, folded back into the tree:** (a) inline `mcpServers` in `plugin.json` is silently IGNORED by the runtime (validate accepts it — a validator/runtime gap); the referee moved to `.mcp.json` at plugin root, which is honored — accepting the dual-meaning caveat that this file is also project MCP config when the detent repo itself is opened in claude (harmless: no `.detent/config.json` here → the server refuses to serve). (b) `permissionMode` in plugin agent frontmatter is IGNORED (WARN per file per session; honored only for `.claude/agents/`) — the key is stripped from the T-112 generator (dead config does not ship; the read-only surface rides the `tools` allowlist, and the SDK path still sets the real mode), a below-decision refinement to T-112's frontmatter list forced by execution (P4). (c) In this build, `--plugin-dir` loads hooks/skills/agents but does NOT start plugin MCP servers, and plugin MCP tools did not surface inside print-mode sessions even for the installed plugin (server health-checks Connected; tools absent from the session's tool list) — recorded for **T-120/MP2**, where in-session referee tools become load-bearing: verify interactively first; the fallbacks are project-scope `.mcp.json` written by init or `--mcp-config` at launch. MP1 exit criteria per this plan and PRD §13 are met: manifest + marketplace validated and installed, two commands live, four agents vendored, containment hook proven denying in a real session.

**3.0-6** — **MP1's keyless body lands: T-110…T-113; T-114 ⚡ stays open as the live exit.** T-110: `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json`, component dirs at plugin root, referee bundled as the `referee` MCP server **inline in the manifest** rather than a root `.mcp.json` — a root file would double as this repo's own project MCP config and spawn a referee in every unrelated session here (dual-meaning file avoided). Version is one deliberate string (SEC-2): `REFEREE_VERSION` exported from `server.ts`, manifest pinned to it by test. `claude plugin validate --strict` green on all four targets (marketplace manifest, plugin manifest, `skills/`, `agents/`), wrapped as `npm run plugin:validate` (local AC — CI runners have no claude CLI). `userConfig` weighed per the research and deliberately NOT adopted: it is PRDR-062's candidate resolution and that PRDR decides, not this ticket. T-111: commands as skills (`skills/init/SKILL.md`, `skills/run/SKILL.md`, `$ARGUMENTS`), bodies carrying the binding contracts — init enumerates the five C-5 decisions by name; run names the four C-11 outcomes, all eight R-1 tools, and the D-28 no-ambient-bypass rule; docs tests pin the two-command/five-decision surface (C-14′). Both bodies state MP1 honestly: status/read-only behavior now, sequencing activates at MP2 (init phases at MP3). T-112: `scripts/build-plugin.ts` (`npm run plugin`) renders `agents/{diagnose,implement,review,research}.md` — the body is the S-7-pinned prompt **byte-for-byte** (`loadPromptSet` verifies every pin before rendering; tests re-hash the shipped body against `prompts/manifest.json` and resolve `role@hash`), frontmatter derives from the same sources the SDK backend uses (`toolsForRole` → `tools`, `READ_ONLY_ROLES` → `permissionMode: plan`, X-1 `turns_per_stage` → `maxTurns: 30`, `disallowedTools: Task` per D-28) — one truth, three skins. Research ships the domain-less web surface while PRDR-062 is open (narrower, never wider). T-113: the hook ships as a **`command` hook**, not `mcp_tool` — an `mcp_tool` guard would add a ninth tool to R-1's closed enumerated set, and the command form is provable key-free in the oracle's own subprocess shape. One legality implementation is preserved by **bundling** `guardToolUse`/`stopGate` (esbuild → self-contained CJS `hooks/dist/detent-hook.cjs`, committed + staleness-tested, runs under bare `node` from any user cwd; esbuild 0.28.2 pinned exact). Protocol: stdin payload → JSON decision on stdout, exit 0 — **deny or silence, never an explicit allow** (D-29: the hook only narrows). Reads the oracle's F-1 names `.detent/active_surface.json`/`.detent/stage.json` at the session cwd; one documented deviation — an ABSENT surface file is silence, because the ambient plugin hook (unlike the oracle's per-worker install) runs in sessions with no Detent attempt in flight; a PRESENT-but-unreadable surface still fails closed (P5). The T-046 guard semantics re-run against the shipped bundle as subprocesses (15 cases incl. the loop-guard proving the gate did not run). Trust boundary recorded in the module doc: both files are repo content; the surface file can only narrow; a hostile `stage.json` executes `gate_cmd` — exactly the capability repo settings-hooks already have on the same events (research A.4.2), so nothing widens; the referee must write AND clear both files per attempt (MP2, T-120/T-121). **Ship-audit catches, fixed pre-commit:** `.gitignore`'s unanchored `dist/` silently ignored `hooks/dist/` — the committed bundle would never reach a clone and CI staleness would fail on ENOENT (anchored to `/dist/`); `src/plugin/**` joined the sessions lint zone and gained an arch test asserting it imports **nothing** from `src/kernel/**`. Suite: **603 passed / 2 skipped** (+34), all gates green. Folded into T-114's live checklist: T-111's load-in-session AC, and verifying the platform spawns plugin MCP servers with cwd = the user's project (the referee's `--root` default assumes it).

**3.0-5** — the comment rule finished at full strength: `//` is now banned outright in `.ts` files, enforced by a custom flat-config lint rule (`detent/no-line-comments`; the only escape is compiler/lint directives, which TypeScript honors solely in line form). All ~640 remaining line comments across 95 files were converted, preserving their content: runs adjacent to a declaration became that declaration's `/** */` doc-block (275 runs), mid-flow constraint comments became standalone `/* */` blocks (145). The first conversion pass hand-rolled a TypeScript scanner and silently missed comments after template literals — the replacement drives conversions from ESLint's own lexer positions, and the lint rule itself is now the convergence proof. Suite green throughout: 569 passed / 2 skipped.

**3.0-4** — engineering standards codified in `AGENTS.md` and applied tree-wide. Lint-enforced: `max-lines` (300 src / 600 tests, code lines), `no-inline-comments` (54 trailing comments lifted to their own lines), modern-syntax floor (`prefer-const`, `no-var`, `prefer-template`, `object-shorthand`, `prefer-arrow-callback`, `eqeqeq`, `consistent-type-imports`, no enums, no default exports). The two over-limit files split by responsibility, not mechanically: `referee.ts` (769 → 298) now composes `referee-context.ts` (shared ground: config, journal, spend, work dirs), `referee-session.ts` (the session arm — the one `backend.run` path with its launch gate/backstop/prefix-pin/base-guard), `referee-gate.ts` (drift assertion, scoped gates, flake filter, close-check risk), and `referee-stage.ts` (the validator-backed diagnose/review/research launches); `run.ts` (327 → 110) keeps the public entry and `kernel/driver.ts` (222) holds the loop. Arms PROVE (they return sealed events); only the core escrows and admits — the apply-site audit still finds exactly one `apply(` in `referee.ts`, and `kernel/driver.ts` joined the T-102 drivers-hold-nothing scan. `AGENTS.md` doubles as the rules file `readRules()` feeds Detent's own sessions (N-7). Full suite green through the refactor: 569 passed / 2 skipped, all four gates clean.

**3.0-3** — **MP0 lands: T-100…T-107, the referee extraction.** The v2 kernel's primitives moved from the run loop into `src/kernel/referee.ts` (`RefereeCore`) — the ONE `machine.apply` site — fronted by the R-1 tool registry (`src/referee/registry.ts`: 8 tools, zod I/O, structured errors) and an MCP stdio server (`src/referee/server.ts`, entry `src/cli/referee.ts`, plumbing verb `detent referee`). `run.ts` is now the **headless driver**: same public surface, every move through `callTool`, and it imports neither the machine nor the event constructors — the T-102 audit extension asserts drivers hold no apply, no constructor import, and no event-name string. The **evidence escrow** is the boundary mechanism: gates/validators/human-acts mint opaque single-use refs; `transition` redeems them; sealed `KernelEvent`s never cross out of the kernel. Escrow refs are **ticket-bound AND state-bound** (both bindings added by the ship audit: an unbound ref could move a different ticket; an un-state-bound ref could be hoarded at `IN_PROGRESS` and redeemed at `APPROVED`, skipping the close-check's five slots), and a **refused** transition does not consume evidence. Fourth pinned runtime dep: `@modelcontextprotocol/sdk` 1.30.0 (the plugin needs a real spawned stdio process; the Agent SDK's in-process helper covers only the headless side). One error-code rename forced by the extended audit itself: the drift structured error is `DRIFT_HALT`, because `GATE_DRIFT` would have put an event name in the driver's mouth. **Exit evidence:** the entire v2 suite passes through driver→registry→core unchanged — 568 passed / 2 skipped (45 files, +15 new referee/audit tests) — and the T-106 transport fixture proves the stdio server and the in-process registry produce field-identical results and journals on twin repos. Deliberate MP0 scope note: the Agent-SDK `options.plugins` load proof is T-114's live exit; MP0's key-free transport parity stands in for it, as recorded in the research doc (A.4.3). Parity map annotated: the oracle tests now certify the referee.

**3.0-2** — platform + landscape research applied (`docs/research/plugin-platform-and-landscape.md`). Every load-bearing v3 assumption confirmed against official docs — hook deny (v2.1.195+), referee bundling, headless/SDK plugin loading, marketplace schema, and the D-29 crux (documented hook combination rule: most restrictive wins, `deny` > `defer` > `ask` > `allow`; hook-allow cannot bypass denies) — zero PRDRs forced. Refinements folded into T-106 (SDK loads the same plugin dir — one artifact, two drivers), T-110 (root layout, explicit version, `userConfig` as PRDR-062 candidate), T-111 (commands are skills), T-112 (richer role frontmatter), T-113 (guard as `command`/`mcp_tool` calling referee `guard.check`; `prompt`/`agent` hook types P2-forbidden), T-114 (`--plugin-dir`, no marketplace), T-120 (Stop-hook re-feed option, ralph-wiggum precedent), T-121 (platform budget caps as defense in depth), T-122 (hostile-hook fixture now confirms a documented rule), T-141 (three-channel distribution + positioning). Landscape: the enforcement layer is unclaimed at scale (closest analogues 24★/588★/7★); the methodology giants are complementary upstream; platform absorption commoditizes bare loop+budgets, leaving exactly Detent's core as the moat.

**3.0-1** — plan created from `detent-prd-v3.md` (3.0-draft.1); MP0…MP4 decomposed into T-100…T-142; MP0 (referee extraction) detailed, MP1–MP4 at milestone-exit grain pending MP0 landing. Applies PRDR-065.
