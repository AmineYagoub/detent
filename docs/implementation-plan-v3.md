# Detent v3 — Implementation Plan (the plugin re-target)

Source of truth: `detent-prd-v3.md` (3.0-draft.1). This plan decomposes the MP0…MP4 milestone series of §13 into tickets `T-1##` (distinct from the v2 line's `T-0##`). It applies **PRDR-065** and depends on nothing outside the v3 PRD.

## §0. Working agreements (carry from v2, unchanged)
- **No-deviation (N-6).** A design change files a `tickets/prd-review/` PRDR **first**, the PRD amends, then this plan and code follow. PRDR evidence blocks quote the PRD verbatim and are never retro-edited.
- **Trailers.** Commits carry `Detent-Ticket: T-1##`.
- **Tests-before-code, where an oracle exists.** The v2 kernel *is* the referee, and its **553 passing tests are the referee's conformance floor** — MP0 may not regress one. v3's *new* surfaces (the R-* MCP boundary, the plugin, the model-driven loop) have **no oracle**; they are spec-first, tests derived from the PRD's ACs.
- **Live-key discipline (R-10).** Any ticket that spends against the user's `ANTHROPIC_API_KEY` (marked ⚡ below) is gated on explicit per-run consent and a spend cap. Never run unprompted.
- **Code standards live in `AGENTS.md`** — ≤300 code lines per `src/` file (≤600 in tests), no inline comments, modern-syntax floor — with the lint-enforced half in `eslint.config.js` (`max-lines`, `no-inline-comments`, `prefer-const`/`no-var`/`prefer-template`/`object-shorthand`/`eqeqeq`, `consistent-type-imports`, no enums, no default exports). The same file is what `readRules()` feeds Detent's own sessions at self-build (N-7).

## §1. What v3 reuses vs replaces
**Reused unchanged, behind the referee's MCP boundary:** the state machine (§7), budgets (X-1), gate classifier, flake filter, checkpoints (F-4), ticket store, verification adapter (§6), and `schemas/**`. These carry their v2 tests forward verbatim.
**Replaced:** the kernel run loop (`src/kernel/run.ts` orchestration) and the CLI verbs (`src/cli/*`) — split into a driver-agnostic **referee** (MCP tools) plus **two drivers** (headless deterministic loop; interactive model-driven loop) and the **plugin** shell.
**The load-bearing invariant (D-27/ARCH-1):** every `machine.apply` still derives its event from a validator or gate result — now asserted at the *tool* boundary, true regardless of which driver called the tool.

---

## §2. Milestones & tickets

### MP0 — the referee (extract the v2 kernel behind MCP; headless driver at full parity)
The de-risking milestone: prove the kernel runs **unchanged** behind a tool boundary, with the deterministic driver reproducing today's behavior byte-for-byte. No live key anywhere in MP0 — it runs against the existing mock/fixtures.
- **T-100** Referee MCP scaffold (R-1): tool server skeleton, tool registry, zod I/O schemas reusing `src/schemas/**`. *AC:* server lists exactly the R-1 tool set; unknown tool → structured error.
- **T-101** `next` + `claim` tools (R-2): ready-set from the ticket store; atomic claim honoring X-3 admissibility. *AC:* two-ready fixture — either claimable; blocked-ticket claim refused naming the blocker.
- **T-102** `record` + `transition` tools (the sole `machine.apply` path): each tool gate-/validator-derives its event; the ARCH-1 apply-site audit is **extended to the tool boundary**. *AC:* `transition` with an inadmissible event refused, no checkpoint written; audit test asserts no tool reaches `machine.apply` without a validator/gate result.
- **T-103** `gate` tool: run + classify a bound gate via the existing adapter + flake filter (F/V reuse). *AC:* the v2 gate/flake tests pass through the tool unchanged.
- **T-104** `attempt` tool (R-4): metered billable-session spawn wrapping the existing `SessionBackend`; ledger check-before / record-after; over-ceiling refusal routes to a human (P6). *AC:* over-budget fixture refuses and routes; ledger sums every session (mock backend).
- **T-105** Checkpoint/resume through the referee (R-3, D-30): every admitted transition persisted to `.detent/` before the tool returns. *AC:* kill mid-`attempt`; re-drive resumes from the last admitted transition; stale claim reclaimable (C-9).
- **T-106** The **headless driver**: re-express `run.ts`'s loop as a driver over R-* tools — implemented as an Agent SDK program loading **the same plugin directory** (`options.plugins: [{type:"local", path}]`), so one artifact serves both drivers. *AC:* byte-identical `transitions.jsonl` vs the v2 loop on the oracle crash-resume class; the fixture doubles as proof that plugin-bundled MCP + hooks behave identically under SDK load.
- **T-107** **MP0 exit** — the entire v2 suite (553 tests) green with the headless driver over the referee; ARCH-2 driver-agnostic audit; parity map annotated (the oracle tests now certify the referee). *Exit gate for MP1.*

### MP1 — the plugin skeleton
- **T-110** Plugin manifest (`.claude-plugin/plugin.json`) + `marketplace.json`. Component dirs live at plugin **root** (only `plugin.json` inside `.claude-plugin/`); `version` pinned explicitly (SEC-2); `userConfig` weighed as the home for spend cap + docs-domains (candidate resolution for PRDR-062). *AC:* `claude plugin validate --strict` passes; registers exactly two commands.
- **T-111** The two commands (`/detent:init`, `/detent:run`) as **skills** — `skills/init/SKILL.md`, `skills/run/SKILL.md` (directory form; `commands/` is legacy), `$ARGUMENTS` carrying flags like `--replan`. *AC:* both load in a live Claude Code session.
- **T-112** Vendored subagents: the role prompts (diagnose/implement/review/research) → `agents/*.md`, hash-pinned (S-7), using per-role `tools`/`disallowedTools`/`permissionMode`/`maxTurns` frontmatter for S-3′ surfaces. *AC:* `role@hash` resolves; runtime never fetches (SEC-2).
- **T-113** The D-21 containment hook as a plugin hook (`hooks/hooks.json` PreToolUse) + Stop-gate hook. Deterministic hook types only (`command` or `mcp_tool` calling a referee `guard.check` tool — one legality implementation, ARCH-2); the platform's `prompt`/`agent` hook types are P2-forbidden for containment. *AC:* the M0 guard tests pass against the plugin hook.
- **T-114 ⚡ MP1 exit** — via `claude --plugin-dir` (no marketplace needed): commands load and the hook denies an out-of-surface write in a **live** session. *Live-key gated.*

### MP2 — the model-driven loop
- **T-120** The model-driven driver: skill(s) that sequence `next → claim → attempt → record/gate → transition` over R-* tools, choosing among *legal* moves only (R-2). Loop persistence may use the **Stop-hook re-feed** pattern (officially shipped precedent: ralph-wiggum) rather than trusting the model to continue — it coexists with the S-2 stop-gate, since Stop hooks combine. *AC:* the model completes a single-ticket run over the referee against a fixture backend.
- **T-121** D-28 budget hook: deny ledger-bypassing ambient billable tools (direct `Task`/gate-running `Bash`); `attempt` is the sole billable path; the platform's native subagent budget caps layer underneath as defense in depth (Detent's ledger stays the source of record). *AC:* ambient spawn for Detent work hook-denied; ledger sees every attempt.
- **T-122** D-29 / SEC-6 hook authority over loaded settings — confirming the documented combination rule ("most restrictive applies: `deny` > `defer` > `ask` > `allow`"; hook-allow never bypasses denies) by execution, per P4. *AC:* a repo settings file allow-listing an out-of-surface write is hook-denied; a repo-authored *hook* answering "allow" does not preempt the guard's deny; no transition recorded in either case.
- **T-123** Cross-driver parity harness: a multi-ticket run under the model driver yields `transitions.jsonl` byte-identical to the headless driver for the same admitted sequence (scripted/fixture backend for determinism). *AC:* parity fixture green.
- **T-124 ⚡ MP2 exit** — a live multi-ticket model-driven run completes; budgets provably hard. *Live-key gated.*

### MP3 — init as a plugin
- **T-130** The seven init phases surfaced as command/skills; the five C-5 decisions as the plugin's presented interrupts (closed set). *AC:* interrupt set asserted at five; phase order matches C-4.1.
- **T-131** Approval (C-7) as a plugin presented decision (the `makeTtyApproval` role, re-surfaced). *AC:* approved/declined/deferred map to the same three outcomes; approval records who/when/plan_hash.
- **T-132 MP3 exit** — the golden-path docs test passes against the plugin surface (two commands, five decisions).

### MP4 — self-build + distribution (N-7)
- **T-140 ⚡** The headless driver self-builds v3 in CI — the permanent gate (D-16), now naming `detent-prd-v3.md`. *Live-key gated.*
- **T-141 ⚡** Marketplace publish + install smoke test — three channels in order: own marketplace repo (immediate; any git repo serves), submission to the reviewed community marketplace (security-scan queue), claude.com/plugins listing. Positioning per research C.2: lead with the four unclaimed axes; frame methodology tools (spec-kit, Superpowers, BMAD) as complementary upstream. *Live-key gated.*
- **T-142 MP4 exit / v3 release gate** — N-7 green on the v3 document; plugin installs and runs the golden path.

---

## §3. Critical path
`T-100 → 101 → 102 → 103 → 104 → 105 → 106 → 107` (MP0, the long pole — the referee must be proven at parity before anything drives it) `→ 110 → 111 → 113 → 114` (MP1) `→ 120 → 121 → 123 → 124` (MP2) `→ 130 → 132` (MP3) `→ 140 → 142` (MP4). MP0 is ~8 tickets of pure reuse-behind-a-boundary; the genuinely new risk concentrates in T-102 (apply-site invariant at the tool boundary), T-121 (budget hardness under ambient tools — OQ-A in practice), and T-123 (proving the two drivers agree).

## §4. Open risks carried from PRDR-065
- **OQ-A budget authority** is validated (or falsified) in **T-121**. If the hook cannot make budgets hard, the fallback is D-25's stated overshoot bound, documented, not silent.
- **OQ-B unattended runs** is answered by the headless driver (MP0) surviving as the CI path — already in the plan.
- **OQ-C D-22 replacement** is validated in **T-122/SEC-6**.
- **OQ-D resumability** is validated in **T-105** (referee-owned checkpoints) and **T-123** (cross-driver agreement).

## §5. Changelog
**3.0-9** — **T-124 ⚡ MP2 exit: GREEN — MP2 complete.** Live evidence (CLI 2.1.235, installed-plugin hooks+skills, harness referee via `--mcp-config --strict-mcp-config` with the GREEN-scripted mock backend — the model is the live half, the workers stay fixture-backed; full live workers remain T-140/N-7 — nine haiku sessions, ≈$0.47 total): **(1) the live multi-ticket model-driven run completes** — `/detent:run` executed by a real model drove t-1 and t-2 through claim → transition → attempt → gate → transition → review record → transition → close-check → transition → finalize → close → release, 30 referee tool calls in exactly the skill's order, outcome `ok`, both DONE — and the run's `transitions.jsonl` is **identical row-for-row (at-stripped; two wall clocks) to a headless twin**, which together with T-123's fixed-clock byte-identity discharges the §13 exit sentence. **(2) Budgets provably hard**: on an exhausted-ledger fixture the model's `attempt` was refused BEFORE any spawn (ledger still exactly one row), the model followed the breach route verbatim — record breach → transition → `BUDGET_BREACH → NEEDS_HUMAN` journaled, dossier written — then presented the four C-10 choices and STOPPED, holding the claim; in print mode that abandoned claim is precisely D-30's interactive-abandonment resume case, observed live. **(3) Ambient bypass denied live**: gate-command Bash, a file write (D-27 reason), and billable spawns all refused in-session under an active policy. The spawn leg found a real gap: this build's print sessions expose NO tool named `Task` — the live spawn surface is `TaskCreate` (background tasks) and `Agent` — so `deny_tools: ["Task"]` guarded yesterday's platform. Fixed at the referee (`BILLABLE_SPAWN_TOOLS = ["Task", "Agent", "TaskCreate"]` in hook-policy.ts; reads/controls stay allowed), then re-proven live: both new names denied with the D-28 reason. The hook itself needed no change — the deny list is policy data, which is the design working. **(4) SEC-6 platform merge confirmed by execution**: a repo shipping allow-everything permissions AND a repo-authored PreToolUse allow-hook (proven to have RUN via a marker file it wrote), under `--permission-mode acceptEdits`, still could not move the guard — write denied with the D-27 reason, no file created, no transition recorded. **Platform fact resolved**: 2.1.235 surfaces installed-plugin MCP tools in print-mode sessions (all eight, `mcp__plugin_detent_referee__*`) — 3.0-7's gap was version-bound; note the plugin-bundled referee still defaults `--backend claude`, which is why the deterministic legs ran the harness server. One incidental: a first SEC-6 attempt used "pwned" as the payload and the inner model refused on its own suspicion before any tool call — fixture prompts stay innocuous. **MP2 exit criteria per §13 are met.** Next: MP3 (T-130…T-132, keyless) or MP4; v2's T-051/T-070/T-071 (live workers) remain open alongside.

**3.0-8** — **MP2's keyless body lands: T-120…T-123; T-124 ⚡ stays open as the live exit.** T-120: `skills/run/SKILL.md` is now the model driver's full program — the state→move table mirroring the deterministic loop (attempt/gate/record/transition per state, the D-13 escalate-reason literal, BREACH and DRIFT_HALT as routes, terminal bookkeeping, escalation presented to the human and never answered for them), plus the referee-written **hook-policy files** the 3.0-6 obligation called for: `acquire` publishes `.detent/active_surface.json` (driver form: `driver: true`, EMPTY surface — the driver sequences, never edits — D-27 made mechanical at the hook), `releaseTicket` and the drift unwind clear it, and `pool()` maintains `.detent/stage.json` as the run-scoped **Stop re-feed** (ralph-wiggum precedent): standing while work remains, gone the moment it is not, one firing bounded by `stop_hook_active`. Both files are TTL-bounded by the X-1 wall-clock ceiling (`expires_at_ms`; the hook treats an expired policy as absent) so a crashed driver cannot leave a repo's ordinary sessions denied — the residual is a ≤1 h stale-deny window matching the claim's own staleness, documented. Both files joined F-1's LAYOUT as `local`/`run-level` entries, so the derived `.detent/.gitignore` keeps them out of ticket commits by construction (no PRD decision touched; flagged for the eventual §5 text refresh). T-121: the bundle now enforces D-28's ambient-bypass denies from the policy file — `deny_tools: ["Task"]` (attempt is the sole billable path) and `deny_bash_containing` = the bound verification commands (substring match on the Bash command; deny-only accelerant, so a false positive is a safe refusal and an evaded string still moves nothing — gates only classify through the referee). Known honest bound: wall-clock hardness under the model driver rides the policy TTL + the referee's ledger checks, not a driver-side timer. T-122 (keyless halves): a hostile-repo fixture — settings allow-listing everything plus a repo-authored allow-hook — cannot move our hook (denies unchanged, nothing written, and a source scan pins that the hook and guard never read a settings surface); the platform-merge half (documented deny-beats-allow combination) is execution-only and sits on **T-124's live checklist**. T-123: `tests/plugin/skill-driver.ts` — a scripted model driver that executes the SKILL's program literally over `callTool`, deliberately independent of `kernel/driver.ts`, and **now inside the T-102 drivers-hold-nothing audit scan** — drives twin repos against the headless driver under a fixed injected clock and the same run id: **`transitions.jsonl` is byte-identical**, timestamps included, on a two-ticket run (and a single-ticket run completes to DONE using only the eight R-1 tools). One fixture correction forced by the new referee clock reads: the T-041 wall-clock fake armed itself by CALL ORDER ("first reading arms"), which referee-internal TTL reads now shift — re-expressed as stage-armed time (the implement attempt consumes the ceiling), same oracle claim, no ordering coupling; BUDGET_BREACH is legal from every non-DONE state so the assertion set is unchanged. Docs locks added: the skill body must name every state the harness handles, and the D-13 literal must be ONE string across driver, harness, and skill. Suite: **622 passed / 2 skipped** (51 files, +19), all gates green, `plugin:validate` ×4. MP2 exit criteria remaining for **T-124 ⚡**: a live multi-ticket model-driven run (the skill executed by a real model over in-session referee tools — includes resolving 3.0-7's platform fact that plugin MCP tools did not surface in print-mode sessions), the live over-budget refusal routing to a human, the live ambient-bypass deny, and the SEC-6 platform-merge fixture.

**3.0-7** — **T-114 ⚡ MP1 exit: GREEN — MP1 complete.** Live evidence (CLI 2.1.233→2.1.235, claude.ai auth, eight haiku print-mode sessions, ≈$0.24 total): **(1) commands load** — debug log `Loaded 2 skills from plugin detent`; `/detent:init` invoked live applied C-1 by name (refused a non-git dir, created nothing, routed to the headless driver) and `/detent:run` presented the fixture plan faithfully and refused to sequence without the referee, exactly as its MP1 body instructs. **(2) The hook denies an out-of-surface write in a live session** — with `.detent/active_surface.json` planted (`src/**`), a root-level `notes.md` write was refused with the SEC-3 reason verbatim and the file never created, **under `--permission-mode acceptEdits`** — a live demonstration of D-29's deny-over-allow; the in-surface control (`src/notes.md`) wrote clean, proving the deny is surgical. **(3) The cwd assumption holds** — `claude mcp list` from a fixture project shows `plugin:detent:referee ✔ Connected`, and since `src/cli/referee.ts` exits before serving when `--root` (default cwd) has no config, a completed handshake proves plugin MCP servers spawn with cwd = the user's project. Marketplace path exercised for real: `claude plugin marketplace add <repo>` + `claude plugin install detent@detent` both clean (then uninstalled to restore the user's config). **Three platform facts, folded back into the tree:** (a) inline `mcpServers` in `plugin.json` is silently IGNORED by the runtime (validate accepts it — a validator/runtime gap); the referee moved to `.mcp.json` at plugin root, which is honored — accepting the dual-meaning caveat that this file is also project MCP config when the detent repo itself is opened in claude (harmless: no `.detent/config.json` here → the server refuses to serve). (b) `permissionMode` in plugin agent frontmatter is IGNORED (WARN per file per session; honored only for `.claude/agents/`) — the key is stripped from the T-112 generator (dead config does not ship; the read-only surface rides the `tools` allowlist, and the SDK path still sets the real mode), a below-decision refinement to T-112's frontmatter list forced by execution (P4). (c) In this build, `--plugin-dir` loads hooks/skills/agents but does NOT start plugin MCP servers, and plugin MCP tools did not surface inside print-mode sessions even for the installed plugin (server health-checks Connected; tools absent from the session's tool list) — recorded for **T-120/MP2**, where in-session referee tools become load-bearing: verify interactively first; the fallbacks are project-scope `.mcp.json` written by init or `--mcp-config` at launch. MP1 exit criteria per this plan and PRD §13 are met: manifest + marketplace validated and installed, two commands live, four agents vendored, containment hook proven denying in a real session.

**3.0-6** — **MP1's keyless body lands: T-110…T-113; T-114 ⚡ stays open as the live exit.** T-110: `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json`, component dirs at plugin root, referee bundled as the `referee` MCP server **inline in the manifest** rather than a root `.mcp.json` — a root file would double as this repo's own project MCP config and spawn a referee in every unrelated session here (dual-meaning file avoided). Version is one deliberate string (SEC-2): `REFEREE_VERSION` exported from `server.ts`, manifest pinned to it by test. `claude plugin validate --strict` green on all four targets (marketplace manifest, plugin manifest, `skills/`, `agents/`), wrapped as `npm run plugin:validate` (local AC — CI runners have no claude CLI). `userConfig` weighed per the research and deliberately NOT adopted: it is PRDR-062's candidate resolution and that PRDR decides, not this ticket. T-111: commands as skills (`skills/init/SKILL.md`, `skills/run/SKILL.md`, `$ARGUMENTS`), bodies carrying the binding contracts — init enumerates the five C-5 decisions by name; run names the four C-11 outcomes, all eight R-1 tools, and the D-28 no-ambient-bypass rule; docs tests pin the two-command/five-decision surface (C-14′). Both bodies state MP1 honestly: status/read-only behavior now, sequencing activates at MP2 (init phases at MP3). T-112: `scripts/build-plugin.ts` (`npm run plugin`) renders `agents/{diagnose,implement,review,research}.md` — the body is the S-7-pinned prompt **byte-for-byte** (`loadPromptSet` verifies every pin before rendering; tests re-hash the shipped body against `prompts/manifest.json` and resolve `role@hash`), frontmatter derives from the same sources the SDK backend uses (`toolsForRole` → `tools`, `READ_ONLY_ROLES` → `permissionMode: plan`, X-1 `turns_per_stage` → `maxTurns: 30`, `disallowedTools: Task` per D-28) — one truth, three skins. Research ships the domain-less web surface while PRDR-062 is open (narrower, never wider). T-113: the hook ships as a **`command` hook**, not `mcp_tool` — an `mcp_tool` guard would add a ninth tool to R-1's closed enumerated set, and the command form is provable key-free in the oracle's own subprocess shape. One legality implementation is preserved by **bundling** `guardToolUse`/`stopGate` (esbuild → self-contained CJS `hooks/dist/detent-hook.cjs`, committed + staleness-tested, runs under bare `node` from any user cwd; esbuild 0.28.2 pinned exact). Protocol: stdin payload → JSON decision on stdout, exit 0 — **deny or silence, never an explicit allow** (D-29: the hook only narrows). Reads the oracle's F-1 names `.detent/active_surface.json`/`.detent/stage.json` at the session cwd; one documented deviation — an ABSENT surface file is silence, because the ambient plugin hook (unlike the oracle's per-worker install) runs in sessions with no Detent attempt in flight; a PRESENT-but-unreadable surface still fails closed (P5). The T-046 guard semantics re-run against the shipped bundle as subprocesses (15 cases incl. the loop-guard proving the gate did not run). Trust boundary recorded in the module doc: both files are repo content; the surface file can only narrow; a hostile `stage.json` executes `gate_cmd` — exactly the capability repo settings-hooks already have on the same events (research A.4.2), so nothing widens; the referee must write AND clear both files per attempt (MP2, T-120/T-121). **Ship-audit catches, fixed pre-commit:** `.gitignore`'s unanchored `dist/` silently ignored `hooks/dist/` — the committed bundle would never reach a clone and CI staleness would fail on ENOENT (anchored to `/dist/`); `src/plugin/**` joined the sessions lint zone and gained an arch test asserting it imports **nothing** from `src/kernel/**`. Suite: **603 passed / 2 skipped** (+34), all gates green. Folded into T-114's live checklist: T-111's load-in-session AC, and verifying the platform spawns plugin MCP servers with cwd = the user's project (the referee's `--root` default assumes it).

**3.0-5** — the comment rule finished at full strength: `//` is now banned outright in `.ts` files, enforced by a custom flat-config lint rule (`detent/no-line-comments`; the only escape is compiler/lint directives, which TypeScript honors solely in line form). All ~640 remaining line comments across 95 files were converted, preserving their content: runs adjacent to a declaration became that declaration's `/** */` doc-block (275 runs), mid-flow constraint comments became standalone `/* */` blocks (145). The first conversion pass hand-rolled a TypeScript scanner and silently missed comments after template literals — the replacement drives conversions from ESLint's own lexer positions, and the lint rule itself is now the convergence proof. Suite green throughout: 569 passed / 2 skipped.

**3.0-4** — engineering standards codified in `AGENTS.md` and applied tree-wide. Lint-enforced: `max-lines` (300 src / 600 tests, code lines), `no-inline-comments` (54 trailing comments lifted to their own lines), modern-syntax floor (`prefer-const`, `no-var`, `prefer-template`, `object-shorthand`, `prefer-arrow-callback`, `eqeqeq`, `consistent-type-imports`, no enums, no default exports). The two over-limit files split by responsibility, not mechanically: `referee.ts` (769 → 298) now composes `referee-context.ts` (shared ground: config, journal, spend, work dirs), `referee-session.ts` (the session arm — the one `backend.run` path with its launch gate/backstop/prefix-pin/base-guard), `referee-gate.ts` (drift assertion, scoped gates, flake filter, close-check risk), and `referee-stage.ts` (the validator-backed diagnose/review/research launches); `run.ts` (327 → 110) keeps the public entry and `kernel/driver.ts` (222) holds the loop. Arms PROVE (they return sealed events); only the core escrows and admits — the apply-site audit still finds exactly one `apply(` in `referee.ts`, and `kernel/driver.ts` joined the T-102 drivers-hold-nothing scan. `AGENTS.md` doubles as the rules file `readRules()` feeds Detent's own sessions (N-7). Full suite green through the refactor: 569 passed / 2 skipped, all four gates clean.

**3.0-3** — **MP0 lands: T-100…T-107, the referee extraction.** The v2 kernel's primitives moved from the run loop into `src/kernel/referee.ts` (`RefereeCore`) — the ONE `machine.apply` site — fronted by the R-1 tool registry (`src/referee/registry.ts`: 8 tools, zod I/O, structured errors) and an MCP stdio server (`src/referee/server.ts`, entry `src/cli/referee.ts`, plumbing verb `detent referee`). `run.ts` is now the **headless driver**: same public surface, every move through `callTool`, and it imports neither the machine nor the event constructors — the T-102 audit extension asserts drivers hold no apply, no constructor import, and no event-name string. The **evidence escrow** is the boundary mechanism: gates/validators/human-acts mint opaque single-use refs; `transition` redeems them; sealed `KernelEvent`s never cross out of the kernel. Escrow refs are **ticket-bound AND state-bound** (both bindings added by the ship audit: an unbound ref could move a different ticket; an un-state-bound ref could be hoarded at `IN_PROGRESS` and redeemed at `APPROVED`, skipping the close-check's five slots), and a **refused** transition does not consume evidence. Fourth pinned runtime dep: `@modelcontextprotocol/sdk` 1.30.0 (the plugin needs a real spawned stdio process; the Agent SDK's in-process helper covers only the headless side). One error-code rename forced by the extended audit itself: the drift structured error is `DRIFT_HALT`, because `GATE_DRIFT` would have put an event name in the driver's mouth. **Exit evidence:** the entire v2 suite passes through driver→registry→core unchanged — 568 passed / 2 skipped (45 files, +15 new referee/audit tests) — and the T-106 transport fixture proves the stdio server and the in-process registry produce field-identical results and journals on twin repos. Deliberate MP0 scope note: the Agent-SDK `options.plugins` load proof is T-114's live exit; MP0's key-free transport parity stands in for it, as recorded in the research doc (A.4.3). Parity map annotated: the oracle tests now certify the referee.

**3.0-2** — platform + landscape research applied (`docs/research/plugin-platform-and-landscape.md`). Every load-bearing v3 assumption confirmed against official docs — hook deny (v2.1.195+), referee bundling, headless/SDK plugin loading, marketplace schema, and the D-29 crux (documented hook combination rule: most restrictive wins, `deny` > `defer` > `ask` > `allow`; hook-allow cannot bypass denies) — zero PRDRs forced. Refinements folded into T-106 (SDK loads the same plugin dir — one artifact, two drivers), T-110 (root layout, explicit version, `userConfig` as PRDR-062 candidate), T-111 (commands are skills), T-112 (richer role frontmatter), T-113 (guard as `command`/`mcp_tool` calling referee `guard.check`; `prompt`/`agent` hook types P2-forbidden), T-114 (`--plugin-dir`, no marketplace), T-120 (Stop-hook re-feed option, ralph-wiggum precedent), T-121 (platform budget caps as defense in depth), T-122 (hostile-hook fixture now confirms a documented rule), T-141 (three-channel distribution + positioning). Landscape: the enforcement layer is unclaimed at scale (closest analogues 24★/588★/7★); the methodology giants are complementary upstream; platform absorption commoditizes bare loop+budgets, leaving exactly Detent's core as the moat.

**3.0-1** — plan created from `detent-prd-v3.md` (3.0-draft.1); MP0…MP4 decomposed into T-100…T-142; MP0 (referee extraction) detailed, MP1–MP4 at milestone-exit grain pending MP0 landing. Applies PRDR-065.
